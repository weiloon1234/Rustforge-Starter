// AUTO-GENERATED FILE — DO NOT EDIT
// Generated by build.rs from Rust model sources + configs.toml
#![allow(dead_code, unused_imports, unused_variables, unused_mut)]
use anyhow::Result;
use serde::{Deserialize, Serialize};
use schemars::JsonSchema;
use sqlx::FromRow;
use core_db::common::sql::{BindValue, Op, OrderDir, SetMode, bind, bind_query, bind_scalar, generate_snowflake_i64, is_sql_profiler_enabled, format_duration, record_profiled_query, DbConn};
use core_db::common::pagination::resolve_per_page;
use core_datatable::{AutoDataTable, BoxFuture, DataTableColumnDescriptor, DataTableContext, DataTableInput, DataTableRelationColumnDescriptor, GeneratedTableAdapter, ParsedFilter, SortDirection};
use core_db::platform::localized::types::LocalizedMap;
use crate::generated::models::common::{FieldChange, FieldInput, Page, log_observer_error, renumber_placeholders};
use core_db::common::model_api::{ColExpr, Column, Create, CreateState, ManyRelation, ModelDef, OneRelation, Patch, PatchState, Query, QueryState};
use super::enums::*;
use core_db::common::model_observer::{ModelEvent, try_get_observer};
const HAS_CREATED_AT: bool = true;
const HAS_UPDATED_AT: bool = true;
const HAS_SOFT_DELETE: bool = true;
use crate :: permissions :: Permission ;
use core_db :: common :: auth :: permissions :: has_permission as granted_has_permission ;
pub fn admin_identity (username : Option < & str > , name : Option < & str > , email : Option < & str > , id : Option < i64 > ,) -> String { if let Some (value) = first_non_empty (username) { return value . to_string () ; } if let Some (value) = first_non_empty (name) { return value . to_string () ; } if let Some (value) = first_non_empty (email) { return value . to_string () ; } id . map (| value | value . to_string ()) . unwrap_or_else (| | "unknown" . to_string ()) }
fn first_non_empty (value : Option < & str >) -> Option < & str > { value . and_then (| v | { let trimmed = v . trim () ; if trimmed . is_empty () { None } else { Some (trimmed) } }) }
pub fn admin_permissions (abilities : & serde_json :: Value) -> Vec < String > { let mut out = Vec :: new () ; if let Some (items) = abilities . as_array () { for item in items { let Some (raw) = item . as_str () else { continue ; } ; let value = raw . trim () ; if value . is_empty () { continue ; } if value == "*" { out . push ("*" . to_string ()) ; continue ; } if let Some (permission) = Permission :: from_str (value) { out . push (permission . as_str () . to_string ()) ; } } } out . sort () ; out . dedup () ; out }

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[doc(hidden)]
pub struct AdminCreate {
    pub id: FieldInput<i64>,
    pub username: FieldInput<String>,
    pub email: FieldInput<Option<String>>,
    pub locale: FieldInput<Option<String>>,
    pub password: FieldInput<String>,
    pub name: FieldInput<String>,
    pub admin_type: FieldInput<AdminType>,
    pub abilities: FieldInput<serde_json::Value>,
    pub created_at: FieldInput<time::OffsetDateTime>,
    pub updated_at: FieldInput<time::OffsetDateTime>,
    pub deleted_at: FieldInput<Option<time::OffsetDateTime>>,
}

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[doc(hidden)]
pub struct AdminChanges {
    pub id: Option<FieldChange<i64>>,
    pub username: Option<FieldChange<String>>,
    pub email: Option<FieldChange<Option<String>>>,
    pub locale: Option<FieldChange<Option<String>>>,
    pub password: Option<FieldChange<String>>,
    pub name: Option<FieldChange<String>>,
    pub admin_type: Option<FieldChange<AdminType>>,
    pub abilities: Option<FieldChange<serde_json::Value>>,
    pub created_at: Option<FieldChange<time::OffsetDateTime>>,
    pub updated_at: Option<FieldChange<time::OffsetDateTime>>,
    pub deleted_at: Option<FieldChange<Option<time::OffsetDateTime>>>,
}

#[derive(Debug, Clone, FromRow, Serialize, Deserialize, JsonSchema)]
#[doc(hidden)]
pub struct AdminRow {
    pub id: i64,
    pub username: String,
    pub email: Option<String>,
    pub locale: Option<String>,
    pub password: String,
    pub name: String,
    pub admin_type: AdminType,
    pub abilities: serde_json::Value,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub created_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub updated_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339::option")]
    #[schemars(with = "String")]
    pub deleted_at: Option<time::OffsetDateTime>,
}

#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct AdminRecord {
    pub id: i64,
    pub username: String,
    pub email: Option<String>,
    pub locale: Option<String>,
    pub password: String,
    pub name: String,
    pub admin_type: AdminType,
    pub abilities: serde_json::Value,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub created_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub updated_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339::option")]
    #[schemars(with = "String")]
    pub deleted_at: Option<time::OffsetDateTime>,
    #[serde(default)]
    pub admin_type_explained: String,
}

impl AdminRecord {
    pub fn update<'db>(&self, db: impl Into<DbConn<'db>>) -> Patch<'db, AdminModel> {
        AdminModel::query(db.into()).where_col(AdminDbCol::Id, Op::Eq, self.id.clone()).patch()
    }
}

fn hydrate_record(row: AdminRow, _loc: &LocalizedMap, _base_url: Option<&str>) -> AdminRecord {
    let mut record = AdminRecord {
        id: row.id,
        username: row.username,
        email: row.email,
        locale: row.locale,
        password: row.password,
        name: row.name,
        admin_type: row.admin_type,
        abilities: row.abilities,
        created_at: row.created_at,
        updated_at: row.updated_at,
        deleted_at: row.deleted_at,
        admin_type_explained: row.admin_type.explained_label(),
    };
    record
}

impl AdminRecord {
    pub fn identity (& self) -> String { admin_identity (Some (self . username . as_str ()) , Some (self . name . as_str ()) , self . email . as_deref () , Some (self . id) ,) }
    pub fn has_permission (& self , permission : Permission) -> bool { if matches ! (self . admin_type , AdminType :: Developer | AdminType :: SuperAdmin) { return true ; } let granted = admin_permissions (& self . abilities) ; granted_has_permission (& granted , permission . as_str ()) }
    pub fn has_any_permissions (& self , permissions : & [Permission]) -> bool { permissions . iter () . copied () . any (| permission | self . has_permission (permission)) }
    pub fn has_all_permissions (& self , permissions : & [Permission]) -> bool { permissions . iter () . copied () . all (| permission | self . has_permission (permission)) }
    pub fn parsed_abilities (& self) -> Vec < Permission > { self . abilities . as_array () . map (| items | { items . iter () . filter_map (| item | item . as_str ()) . filter_map (Permission :: from_str) . collect () }) . unwrap_or_default () }
}

#[derive(Debug, Clone, Copy, Default)]
pub struct AdminCol;
impl AdminCol {
    pub const ID: Column<AdminModel, i64> = Column::new("id");
    pub const USERNAME: Column<AdminModel, String> = Column::new("username");
    pub const EMAIL: Column<AdminModel, Option<String>> = Column::new("email");
    pub const LOCALE: Column<AdminModel, Option<String>> = Column::new("locale");
    pub const PASSWORD: Column<AdminModel, String> = Column::new("password");
    pub const NAME: Column<AdminModel, String> = Column::new("name");
    pub const ADMIN_TYPE: Column<AdminModel, AdminType> = Column::new("admin_type");
    pub const ABILITIES: Column<AdminModel, serde_json::Value> = Column::new("abilities");
    pub const CREATED_AT: Column<AdminModel, time::OffsetDateTime> = Column::new("created_at");
    pub const UPDATED_AT: Column<AdminModel, time::OffsetDateTime> = Column::new("updated_at");
    pub const DELETED_AT: Column<AdminModel, Option<time::OffsetDateTime>> = Column::new("deleted_at");
}

fn resolve_admin_db_col(sql: &str) -> Option<AdminDbCol> {
    match sql {
        "id" => Some(AdminDbCol::Id),
        "username" => Some(AdminDbCol::Username),
        "email" => Some(AdminDbCol::Email),
        "locale" => Some(AdminDbCol::Locale),
        "password" => Some(AdminDbCol::Password),
        "name" => Some(AdminDbCol::Name),
        "admin_type" => Some(AdminDbCol::AdminType),
        "abilities" => Some(AdminDbCol::Abilities),
        "created_at" => Some(AdminDbCol::CreatedAt),
        "updated_at" => Some(AdminDbCol::UpdatedAt),
        "deleted_at" => Some(AdminDbCol::DeletedAt),
        _ => None,
    }
}

#[derive(Debug, Clone, Copy, JsonSchema)]
pub enum AdminDbCol {
    Id,
    Username,
    Email,
    Locale,
    Password,
    Name,
    AdminType,
    Abilities,
    CreatedAt,
    UpdatedAt,
    DeletedAt,
}

impl AdminDbCol {
    pub const fn all() -> &'static [AdminDbCol] {
        &[AdminDbCol::Id, AdminDbCol::Username, AdminDbCol::Email, AdminDbCol::Locale, AdminDbCol::Password, AdminDbCol::Name, AdminDbCol::AdminType, AdminDbCol::Abilities, AdminDbCol::CreatedAt, AdminDbCol::UpdatedAt, AdminDbCol::DeletedAt]
    }
    pub const fn as_sql(self) -> &'static str {
        match self {
            AdminDbCol::Id => "id",
            AdminDbCol::Username => "username",
            AdminDbCol::Email => "email",
            AdminDbCol::Locale => "locale",
            AdminDbCol::Password => "password",
            AdminDbCol::Name => "name",
            AdminDbCol::AdminType => "admin_type",
            AdminDbCol::Abilities => "abilities",
            AdminDbCol::CreatedAt => "created_at",
            AdminDbCol::UpdatedAt => "updated_at",
            AdminDbCol::DeletedAt => "deleted_at",
        }
    }
}


pub struct AdminCreateInner<'db> {
    pub(crate) state: core_db::common::model_api::CreateState<'db>,
}

impl<'db> AdminCreateInner<'db> {
    pub fn new(db: DbConn<'db>, base_url: Option<String>) -> Self {
        Self {
            state: core_db::common::model_api::CreateState::new(db, base_url, "admin"),
        }
    }
    pub fn from_state(state: core_db::common::model_api::CreateState<'db>) -> Self {
        Self {
            state,
        }
    }


pub fn set_id(mut self, val: i64) -> Self {
        self.state = self.state.set_col("id", val.into());
        self
    }
    pub fn set_username(mut self, val: String) -> Self {
        self.state = self.state.set_col("username", val.into());
        self
    }
    pub fn set_email(mut self, val: Option<String>) -> Self {
        self.state = self.state.set_col("email", val.into());
        self
    }
    pub fn set_locale(mut self, val: Option<String>) -> Self {
        self.state = self.state.set_col("locale", val.into());
        self
    }
    pub fn set_password(mut self, val: &str) -> anyhow::Result<Self> {
        let hashed = core_db::common::auth::hash::hash_password(val)?;
        self.state = self.state.set_col("password", hashed.into());
        Ok(self)
    }
    pub fn set_password_raw(mut self, val: String) -> Self {
        self.state = self.state.set_col("password", val.into());
        self
    }
    pub fn set_name(mut self, val: String) -> Self {
        self.state = self.state.set_col("name", val.into());
        self
    }
    pub fn set_admin_type(mut self, val: AdminType) -> Self {
        self.state = self.state.set_col("admin_type", val.into());
        self
    }
    pub fn set_abilities(mut self, val: serde_json::Value) -> Self {
        self.state = self.state.set_col("abilities", val.into());
        self
    }
    pub fn set_created_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.set_col("created_at", val.into());
        self
    }
    pub fn set_updated_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.set_col("updated_at", val.into());
        self
    }
    pub fn set_deleted_at(mut self, val: Option<time::OffsetDateTime>) -> Self {
        self.state = self.state.set_col("deleted_at", val.into());
        self
    }
    pub fn on_conflict_do_nothing(mut self, conflict_cols: &[AdminDbCol]) -> Self {
        self.state = self.state.on_conflict_do_nothing(&conflict_cols.iter().map(|c| c.as_sql()).collect::<Vec<_>>());
        self
    }
    pub fn on_conflict_update(mut self, conflict_cols: &[AdminDbCol]) -> Self {
        self.state = self.state.on_conflict_update(&conflict_cols.iter().map(|c| c.as_sql()).collect::<Vec<_>>());
        self
    }
    fn to_create_input(&self) -> Result<AdminCreate> {
        let mut input = AdminCreate::default();
        for (col_name, bind) in self.state.col_names.iter().zip(self.state.binds.iter()) {
            match *col_name {
                "id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    input.id = FieldInput::Set(value);
                }
                "username" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    input.username = FieldInput::Set(value);
                }
                "email" => {
                    let value = match bind {
                BindValue::StringOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<String>'", other),
            };
                    input.email = FieldInput::Set(value);
                }
                "locale" => {
                    let value = match bind {
                BindValue::StringOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<String>'", other),
            };
                    input.locale = FieldInput::Set(value);
                }
                "password" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    input.password = FieldInput::Set(value);
                }
                "name" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    input.name = FieldInput::Set(value);
                }
                "admin_type" => {
                    let value = match bind {
                BindValue::String(value) => AdminType::from_storage(value)
                    .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'AdminType'", value))?,
                BindValue::I64(value) => {
                    let raw = value.to_string();
                    AdminType::from_storage(&raw)
                        .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'AdminType'", value))?
                }
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'AdminType'", other),
            };
                    input.admin_type = FieldInput::Set(value);
                }
                "abilities" => {
                    let value = match bind {
            BindValue::Json(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'serde_json::Value'", other),
        };
                    input.abilities = FieldInput::Set(value);
                }
                "created_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    input.created_at = FieldInput::Set(value);
                }
                "updated_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    input.updated_at = FieldInput::Set(value);
                }
                "deleted_at" => {
                    let value = match bind {
                BindValue::TimeOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<time::OffsetDateTime>'", other),
            };
                    input.deleted_at = FieldInput::Set(value);
                }
                _ => {}
            }
        }
        Ok(input)
    }


pub async fn save(self) -> Result<AdminRecord> {
        let __create_input = if try_get_observer().is_some() {
            Some(self.to_create_input()?)
        } else {
            None
        };
        if let Some(observer) = try_get_observer() {
            if let Some(create_input) = __create_input.as_ref() {
                let event = ModelEvent { model: "admin", table: "admin", record_key: None };
                let data = serde_json::to_value(create_input)?;
                observer.on_creating(&event, &data).await?;
            }
        }
        let db_conn = self.state.db.clone();
        match db_conn {
            DbConn::Pool(pool) => {
                let tx = pool.begin().await?;
                let tx_lock = std::sync::Arc::new(tokio::sync::Mutex::new(tx));
                let (record, row) = {
                    let db = DbConn::tx(tx_lock.clone());
                    self.save_with_db(db).await?
                };
                let tx = std::sync::Arc::try_unwrap(tx_lock)
                    .map_err(|_| anyhow::anyhow!("transaction scope still has active handles"))?
                    .into_inner();
                tx.commit().await?;
                if let Some(observer) = try_get_observer() {
                    let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", row.id)) };
                    match serde_json::to_value(&row) {
                        Ok(data) => {
                            if let Err(err) = observer.on_created(&event, &data).await {
                                log_observer_error("created", "admin", &err);
                            }
                        }
                        Err(err) => log_observer_error("created", "admin", &err),
                    }
                }
                Ok(record)
            }
            DbConn::Tx(_) => {
                let (record, row) = self.save_with_db(db_conn).await?;
                if let Some(observer) = try_get_observer() {
                    let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", row.id)) };
                    match serde_json::to_value(&row) {
                        Ok(data) => {
                            if let Err(err) = observer.on_created(&event, &data).await {
                                log_observer_error("created", "admin", &err);
                            }
                        }
                        Err(err) => log_observer_error("created", "admin", &err),
                    }
                }
                Ok(record)
            }
        }
    }

    async fn save_with_db<'tx>(mut self, db: DbConn<'tx>) -> Result<(AdminRecord, AdminRow)> {
        if !self.state.col_names.contains(&"id") {
            self.state = self.state.set_col("id", generate_snowflake_i64().into());
        }
        if HAS_CREATED_AT && !self.state.col_names.contains(&"created_at") {
            self.state = self.state.set_col("created_at", time::OffsetDateTime::now_utc().into());
        }
        if HAS_UPDATED_AT && !self.state.col_names.contains(&"updated_at") {
            self.state = self.state.set_col("updated_at", time::OffsetDateTime::now_utc().into());
        }
        if self.state.col_names.is_empty() {
            anyhow::bail!("insert: no columns set");
        }
        let base_url = self.state.base_url.clone();
        let (sql, binds) = self.state.build_insert_sql();
        let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
        let __profiler_start = std::time::Instant::now();
        let mut q = sqlx::query_as::<_, AdminRow>(&sql);
        for b in binds {
            q = bind(q, b);
        }
        let row = db.fetch_one(q).await?;
        record_profiled_query("admin", "INSERT", &sql, &__profiler_binds, __profiler_start.elapsed());
        let localized = LocalizedMap::default();
        let record = hydrate_record(row.clone(), &LocalizedMap::default(), base_url.as_deref());
        Ok((record, row))
    }
}

pub struct AdminPatchInner<'db> {
    pub(crate) state: core_db::common::model_api::PatchState<'db>,
}

impl<'db> AdminPatchInner<'db> {
    pub fn new(db: DbConn<'db>, base_url: Option<String>) -> Self {
        Self {
            state: core_db::common::model_api::PatchState::new(db, base_url, "admin"),
        }
    }
    pub fn from_state(state: core_db::common::model_api::PatchState<'db>) -> Self {
        Self {
            state,
        }
    }


pub fn set_id(mut self, val: i64) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn increment_id(mut self, val: i64) -> Self {
        self.state = self.state.increment_col(AdminDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn decrement_id(mut self, val: i64) -> Self {
        self.state = self.state.decrement_col(AdminDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn set_username(mut self, val: String) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Username.as_sql(), val.into());
        self
    }
    pub fn set_email(mut self, val: Option<String>) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Email.as_sql(), val.into());
        self
    }
    pub fn set_locale(mut self, val: Option<String>) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Locale.as_sql(), val.into());
        self
    }
    pub fn set_password(mut self, val: &str) -> anyhow::Result<Self> {
        let hashed = core_db::common::auth::hash::hash_password(val)?;
        self.state = self.state.assign_col(AdminDbCol::Password.as_sql(), hashed.into());
        Ok(self)
    }
    pub fn set_password_raw(mut self, val: String) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Password.as_sql(), val.into());
        self
    }
    pub fn set_name(mut self, val: String) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Name.as_sql(), val.into());
        self
    }
    pub fn set_admin_type(mut self, val: AdminType) -> Self {
        self.state = self.state.assign_col(AdminDbCol::AdminType.as_sql(), val.into());
        self
    }
    pub fn set_abilities(mut self, val: serde_json::Value) -> Self {
        self.state = self.state.assign_col(AdminDbCol::Abilities.as_sql(), val.into());
        self
    }
    pub fn set_created_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.assign_col(AdminDbCol::CreatedAt.as_sql(), val.into());
        self
    }
    pub fn set_updated_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.assign_col(AdminDbCol::UpdatedAt.as_sql(), val.into());
        self
    }
    pub fn set_deleted_at(mut self, val: Option<time::OffsetDateTime>) -> Self {
        self.state = self.state.assign_col(AdminDbCol::DeletedAt.as_sql(), val.into());
        self
    }
    pub fn where_id(mut self, op: Op, val: i64) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Id.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_username(mut self, op: Op, val: String) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Username.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_email(mut self, op: Op, val: Option<String>) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Email.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_locale(mut self, op: Op, val: Option<String>) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Locale.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_password(mut self, op: Op, val: String) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Password.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_name(mut self, op: Op, val: String) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Name.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_admin_type(mut self, op: Op, val: AdminType) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::AdminType.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_abilities(mut self, op: Op, val: serde_json::Value) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::Abilities.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_created_at(mut self, op: Op, val: time::OffsetDateTime) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::CreatedAt.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_updated_at(mut self, op: Op, val: time::OffsetDateTime) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::UpdatedAt.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_deleted_at(mut self, op: Op, val: Option<time::OffsetDateTime>) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", AdminDbCol::DeletedAt.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_col<T: Into<BindValue>>(mut self, col: AdminDbCol, op: Op, val: T) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", col.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    fn where_raw<T: Into<BindValue>>(mut self, clause: impl Into<String>, binds: impl IntoIterator<Item = T>) -> Self {
        let mut clause = clause.into();
        let incoming: Vec<BindValue> = binds.into_iter().map(Into::into).collect();
        let mut idx = self.state.where_binds.len() + 1;
        while let Some(pos) = clause.find('?') {
            let ph = format!("${}", idx);
            clause.replace_range(pos..pos + 1, &ph);
            idx += 1;
        }
        self.state.where_sql.push(clause);
        self.state.where_binds.extend(incoming);
        self
    }
    fn to_update_changes(&self) -> Result<AdminChanges> {
        let mut changes = AdminChanges::default();
        for ((col, bind), mode) in self.state.set_cols.iter().zip(self.state.set_binds.iter()).zip(self.state.set_modes.iter()) {
            match *col {
                "id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    changes.id = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "username" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    changes.username = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "email" => {
                    let value = match bind {
                BindValue::StringOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<String>'", other),
            };
                    changes.email = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "locale" => {
                    let value = match bind {
                BindValue::StringOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<String>'", other),
            };
                    changes.locale = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "password" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    changes.password = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "name" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    changes.name = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "admin_type" => {
                    let value = match bind {
                BindValue::String(value) => AdminType::from_storage(value)
                    .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'AdminType'", value))?,
                BindValue::I64(value) => {
                    let raw = value.to_string();
                    AdminType::from_storage(&raw)
                        .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'AdminType'", value))?
                }
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'AdminType'", other),
            };
                    changes.admin_type = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "abilities" => {
                    let value = match bind {
            BindValue::Json(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'serde_json::Value'", other),
        };
                    changes.abilities = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "created_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    changes.created_at = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "updated_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    changes.updated_at = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "deleted_at" => {
                    let value = match bind {
                BindValue::TimeOpt(value) => value.clone(),
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'Option<time::OffsetDateTime>'", other),
            };
                    changes.deleted_at = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                _ => {}
            }
        }
        Ok(changes)
    }


pub async fn save(self) -> Result<u64> {
        if self.state.set_cols.is_empty() { anyhow::bail!("update: no columns set"); }
        if self.state.where_sql.is_empty() { anyhow::bail!("update: no conditions set"); }
        let observer_changes = if try_get_observer().is_some() {
            Some(self.to_update_changes()?)
        } else {
            None
        };
        let db_conn = self.state.db.clone();
        match db_conn {
            DbConn::Pool(pool) => {
                let tx = pool.begin().await?;
                let tx_lock = std::sync::Arc::new(tokio::sync::Mutex::new(tx));
                let affected = {
                    let db = DbConn::tx(tx_lock.clone());
                    self.save_with_db(db, observer_changes.clone()).await?
                };
                let tx = std::sync::Arc::try_unwrap(tx_lock)
                    .map_err(|_| anyhow::anyhow!("transaction scope still has active handles"))?
                    .into_inner();
                tx.commit().await?;
                Ok(affected)
            }
            DbConn::Tx(_) => self.save_with_db(db_conn, observer_changes).await,
        }
    }

    async fn save_with_db<'tx>(self, db: DbConn<'tx>, observer_changes: Option<AdminChanges>) -> Result<u64> {
        let mut state = self.state;
        if HAS_UPDATED_AT && !state.set_cols.contains(&AdminDbCol::UpdatedAt.as_sql()) {
            let now = time::OffsetDateTime::now_utc();
            state = state.assign_col(AdminDbCol::UpdatedAt.as_sql(), now.into());
        }
        // find target ids for localized updates
        let select_sql = format!("SELECT id FROM admin WHERE {}", state.where_sql.join(" AND "));
        let mut select_q = sqlx::query_scalar::<_, i64>(&select_sql);
        for b in &state.where_binds { select_q = bind_scalar(select_q, b.clone()); }
        let target_ids = db.fetch_all_scalar(select_q).await?;
        let __observer_active = try_get_observer().is_some();
        let __old_rows: Vec<AdminRow> = if __observer_active && !target_ids.is_empty() {
            let phs: Vec<String> = (1..=target_ids.len()).map(|i| format!("${}", i)).collect();
            let fetch_sql = format!("SELECT * FROM admin WHERE id IN ({})", phs.join(", "));
            let mut fq = sqlx::query_as::<_, AdminRow>(&fetch_sql);
            for id in &target_ids { fq = fq.bind(id); }
            let rows: Vec<AdminRow> = db.fetch_all(fq).await.unwrap_or_default();
            rows
        } else {
            Vec::new()
        };
        if !__old_rows.is_empty() {
            if let Some(observer) = try_get_observer() {
                if let Some(changes) = observer_changes.as_ref() {
                    let changes_data = serde_json::to_value(changes)?;
                    for old_row in &__old_rows {
                        let old_data = serde_json::to_value(old_row)?;
                        let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", old_row.id)) };
                        observer.on_updating(&event, &old_data, &changes_data).await?;
                    }
                }
            }
        }
        let (sql, all_binds) = state.build_update_sql();
        let set_binds = &state.set_binds;
        let mut q = sqlx::query(&sql);
        let __profiler_binds = if is_sql_profiler_enabled() { all_binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
        let __profiler_start = std::time::Instant::now();
        for b in &all_binds { q = bind_query(q, b.clone()); }
        let res = db.execute(q).await?;
        record_profiled_query("admin", "UPDATE", &sql, &__profiler_binds, __profiler_start.elapsed());
        if !__old_rows.is_empty() && res.rows_affected() > 0 {
            if let Some(observer) = try_get_observer() {
                for old_row in &__old_rows {
                    let fetch_sql = format!("SELECT * FROM admin WHERE id = $1");
                    match db.fetch_optional(sqlx::query_as::<_, AdminRow>(&fetch_sql).bind(old_row.id.clone())).await {
                        Ok(Some(new_row)) => {
                            match (serde_json::to_value(old_row), serde_json::to_value(&new_row)) {
                                (Ok(old_data), Ok(new_data)) => {
                                    let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", old_row.id)) };
                                    if let Err(err) = observer.on_updated(&event, &old_data, &new_data).await {
                                        log_observer_error("updated", "admin", &err);
                                    }
                                }
                                (Err(err), _) | (_, Err(err)) => log_observer_error("updated", "admin", &err),
                            }
                        }
                        Ok(None) => {}
                        Err(err) => log_observer_error("updated", "admin", &err),
                    }
                }
            }
        }
        Ok(res.rows_affected())
    }
}



pub struct AdminTableAdapter;
impl AdminTableAdapter {
    fn parse_col(name: &str) -> Option<AdminDbCol> {
        match name {
            "id" => Some(AdminDbCol::Id),
            "username" => Some(AdminDbCol::Username),
            "email" => Some(AdminDbCol::Email),
            "locale" => Some(AdminDbCol::Locale),
            "password" => Some(AdminDbCol::Password),
            "name" => Some(AdminDbCol::Name),
            "admin_type" => Some(AdminDbCol::AdminType),
            "abilities" => Some(AdminDbCol::Abilities),
            "created_at" => Some(AdminDbCol::CreatedAt),
            "updated_at" => Some(AdminDbCol::UpdatedAt),
            "deleted_at" => Some(AdminDbCol::DeletedAt),
            _ => None,
        }
    }
    fn parse_locale_field_for_relation(relation: &str, column: &str) -> Option<&'static str> {
        match (relation, column) {
            _ => None,
        }
    }
    fn parse_locale_field(name: &str) -> Option<&'static str> {
        match name {
            _ => None,
        }
    }
    fn parse_like_col(name: &str) -> Option<AdminDbCol> {
        match name {
            "username" => Some(AdminDbCol::Username),
            "email" => Some(AdminDbCol::Email),
            "locale" => Some(AdminDbCol::Locale),
            "password" => Some(AdminDbCol::Password),
            "name" => Some(AdminDbCol::Name),
            _ => None,
        }
    }
    fn parse_bind_for_col(name: &str, raw: &str) -> Option<BindValue> {
        match name {
            "id" => raw.trim().parse::<i64>().ok().map(Into::into),
            "username" => Some(raw.trim().to_string().into()),
            "email" => Some(Self::parse_bind(raw.trim())),
            "locale" => Some(Self::parse_bind(raw.trim())),
            "password" => Some(raw.trim().to_string().into()),
            "name" => Some(raw.trim().to_string().into()),
            "admin_type" => Some(Self::parse_bind(raw.trim())),
            "abilities" => Some(Self::parse_bind(raw.trim())),
            "created_at" => Self::parse_datetime(raw.trim(), false).map(Into::into),
            "updated_at" => Self::parse_datetime(raw.trim(), false).map(Into::into),
            "deleted_at" => Self::parse_datetime(raw.trim(), false).map(Into::into),
            _ => None,
        }
    }
    fn parse_bind_for_relation(relation: &str, column: &str, raw: &str) -> Option<BindValue> {
        match (relation, column) {
            _ => None,
        }
    }
    fn parse_bind(raw: &str) -> BindValue {
        let trimmed = raw.trim();
        let lower = trimmed.to_ascii_lowercase(); if lower == "true" { return true.into(); } if lower == "false" { return false.into(); }
        if let Ok(v) = trimmed.parse::<i64>() { return v.into(); }
        if let Ok(v) = trimmed.parse::<rust_decimal::Decimal>() { return v.into(); }
        if let Ok(v) = trimmed.parse::<f64>() { return v.into(); }
        if let Ok(v) = uuid::Uuid::parse_str(trimmed) { return v.into(); }
        if let Some(v) = Self::parse_datetime(trimmed, false) { return v.into(); }
        trimmed.to_string().into()
    }
    fn parse_datetime(raw: &str, end_of_day: bool) -> Option<time::OffsetDateTime> {
        let trimmed = raw.trim();
        if let Ok(dt) = time::OffsetDateTime::parse(trimmed, &time::format_description::well_known::Rfc3339) { return Some(dt); }
        if trimmed.len() == 10 {
            let date = time::Date::parse(trimmed, &time::macros::format_description!("[year]-[month]-[day]")).ok()?;
            let t = if end_of_day { time::Time::from_hms(23, 59, 59).ok()? } else { time::Time::MIDNIGHT };
            return Some(date.with_time(t).assume_offset(time::UtcOffset::UTC));
        }
        None
    }
}
impl GeneratedTableAdapter for AdminTableAdapter {
    type Query<'db> = Query<'db, AdminModel>;
    type Row = AdminRecord;
    fn model_key(&self) -> &'static str { "Admin" }
    fn sortable_columns(&self) -> &'static [&'static str] { &["id", "username", "email", "locale", "password", "name", "admin_type", "created_at", "updated_at", "deleted_at"] }
    fn timestamp_columns(&self) -> &'static [&'static str] { &["created_at", "updated_at", "deleted_at"] }
    fn column_descriptors(&self) -> &'static [DataTableColumnDescriptor] {
        &[
            DataTableColumnDescriptor { name: "id", label: "ID", data_type: "i64", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "username", label: "Username", data_type: "String", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "email", label: "Email", data_type: "Option<String>", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "locale", label: "Locale", data_type: "Option<String>", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "password", label: "Password", data_type: "String", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "name", label: "Name", data_type: "String", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "admin_type", label: "Admin Type", data_type: "AdminType", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "abilities", label: "Abilities", data_type: "serde_json::Value", sortable: false, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "created_at", label: "Created At", data_type: "time::OffsetDateTime", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte", "date_from", "date_to"] },
            DataTableColumnDescriptor { name: "updated_at", label: "Updated At", data_type: "time::OffsetDateTime", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte", "date_from", "date_to"] },
            DataTableColumnDescriptor { name: "deleted_at", label: "Deleted At", data_type: "Option<time::OffsetDateTime>", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte", "date_from", "date_to"] },
        ]
    }
    fn relation_column_descriptors(&self) -> &'static [DataTableRelationColumnDescriptor] {
        &[
        ]
    }
    fn filter_patterns(&self) -> &'static [&'static str] {
        &[
            "f-<col>",
            "f-like-<col>",
            "f-gte-<col>",
            "f-lte-<col>",
            "f-date-from-<col>",
            "f-date-to-<col>",
            "f-like-any-<col1|col2|...>",
            "f-any-<col1|col2|...>",
            "f-has-<relation>-<col>",
            "f-has-like-<relation>-<col>",
        ]
    }
    fn apply_auto_filter<'db>(&self, query: Query<'db, AdminModel>, filter: &ParsedFilter, value: &str) -> anyhow::Result<Option<Query<'db, AdminModel>>> where Self: 'db {
        let trimmed = value.trim();
        if trimmed.is_empty() { return Ok(Some(query)); }
        match filter {
            ParsedFilter::Eq { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Eq, bind)))
            }
            ParsedFilter::Like { column } => {
                let Some(col) = Self::parse_like_col(column.as_str()) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Like, format!("%{}%", trimmed))))
            }
            ParsedFilter::Gte { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Ge, bind)))
            }
            ParsedFilter::Lte { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Le, bind)))
            }
            ParsedFilter::DateFrom { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(ts) = Self::parse_datetime(trimmed, false) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Ge, ts)))
            }
            ParsedFilter::DateTo { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(ts) = Self::parse_datetime(trimmed, true) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Le, ts)))
            }
            ParsedFilter::LocaleEq { column } => {
                Ok(None)
            }
            ParsedFilter::LocaleLike { column } => {
                Ok(None)
            }
            ParsedFilter::LikeAny { columns } => {
                let mut applied = false;
                let pattern = format!("%{}%", trimmed);
                let next = query.where_group(|group| {
                    let mut q = group;
                    for column in columns {
                        if let Some(col) = Self::parse_like_col(column.as_str()) {
                            if applied { q = q.or_where_col(col, Op::Like, pattern.clone()); } else { q = q.where_col(col, Op::Like, pattern.clone()); applied = true; }
                        }
                    }
                    q
                });
                if applied { Ok(Some(next)) } else { Ok(None) }
            }
            ParsedFilter::Any { columns } => {
                let mut applied = false;
                let next = query.where_group(|group| {
                    let mut q = group;
                    for column in columns {
                        if let Some(col) = Self::parse_col(column.as_str()) {
                            if let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) { if applied { q = q.or_where_col(col, Op::Eq, bind.clone()); } else { q = q.where_col(col, Op::Eq, bind.clone()); applied = true; } }
                        }
                    }
                    q
                });
                if applied { Ok(Some(next)) } else { Ok(None) }
            }
            ParsedFilter::Has { relation, column } => {
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
            ParsedFilter::HasLike { relation, column } => {
                let pattern = format!("%{}%", trimmed);
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
            ParsedFilter::LocaleHas { relation, column } => {
                let Some(field) = Self::parse_locale_field_for_relation(relation.as_str(), column.as_str()) else { return Ok(None); };
                let locale = core_i18n::current_locale().to_string();
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
            ParsedFilter::LocaleHasLike { relation, column } => {
                let Some(field) = Self::parse_locale_field_for_relation(relation.as_str(), column.as_str()) else { return Ok(None); };
                let locale = core_i18n::current_locale().to_string();
                let pattern = format!("%{}%", trimmed);
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
        }
    }
    fn apply_sort<'db>(&self, query: Query<'db, AdminModel>, column: &str, dir: SortDirection) -> anyhow::Result<Query<'db, AdminModel>> where Self: 'db {
        let dir = match dir { SortDirection::Asc => OrderDir::Asc, SortDirection::Desc => OrderDir::Desc };
        let next = match column {
            "id" => query.order_by(AdminDbCol::Id, dir),
            "username" => query.order_by(AdminDbCol::Username, dir),
            "email" => query.order_by(AdminDbCol::Email, dir),
            "locale" => query.order_by(AdminDbCol::Locale, dir),
            "password" => query.order_by(AdminDbCol::Password, dir),
            "name" => query.order_by(AdminDbCol::Name, dir),
            "admin_type" => query.order_by(AdminDbCol::AdminType, dir),
            "abilities" => query.order_by(AdminDbCol::Abilities, dir),
            "created_at" => query.order_by(AdminDbCol::CreatedAt, dir),
            "updated_at" => query.order_by(AdminDbCol::UpdatedAt, dir),
            "deleted_at" => query.order_by(AdminDbCol::DeletedAt, dir),
            _ => query,
        };
        Ok(next)
    }
    fn apply_cursor<'db>(&self, query: Query<'db, AdminModel>, column: &str, dir: SortDirection, cursor: &str) -> anyhow::Result<Option<Query<'db, AdminModel>>> where Self: 'db {
        let Some(col) = Self::parse_col(column) else { return Ok(None); };
        let Some(bind) = Self::parse_bind_for_col(column, cursor) else { return Ok(None); };
        let op = match dir { SortDirection::Asc => Op::Gt, SortDirection::Desc => Op::Lt };
        Ok(Some(query.where_col(col, op, bind)))
    }
    fn cursor_from_row(&self, row: &AdminRecord, column: &str) -> Option<String> {
        match column {
            "id" => Some(row.id.to_string()),
            "username" => Some(row.username.clone()),
            "email" => row.email.as_ref().map(|v| v.to_string()),
            "locale" => row.locale.as_ref().map(|v| v.to_string()),
            "password" => Some(row.password.clone()),
            "name" => Some(row.name.clone()),
            "created_at" => row.created_at.format(&time::format_description::well_known::Rfc3339).ok(),
            "updated_at" => row.updated_at.format(&time::format_description::well_known::Rfc3339).ok(),
            "deleted_at" => row.deleted_at.as_ref().and_then(|v| v.format(&time::format_description::well_known::Rfc3339).ok()),
            _ => None,
        }
    }
    fn count<'db>(&self, query: Query<'db, AdminModel>) -> BoxFuture<'db, anyhow::Result<i64>> where Self: 'db {
        Box::pin(async move { query.count().await })
    }
    fn fetch_page<'db>(&self, query: Query<'db, AdminModel>, page: i64, per_page: i64) -> BoxFuture<'db, anyhow::Result<Vec<AdminRecord>>> where Self: 'db {
        Box::pin(async move { Ok(query.paginate(page, per_page).await?.data) })
    }
}
#[derive(Debug, Clone, Copy)]
pub struct AdminDataTableConfig {
    pub default_sorting_column: &'static str,
    pub default_sorted: SortDirection,
    pub default_export_ignore_columns: &'static [&'static str],
    pub default_timestamp_columns: &'static [&'static str],
    pub default_unsortable: &'static [&'static str],
    pub default_row_per_page: Option<i64>,
}
impl Default for AdminDataTableConfig {
    fn default() -> Self {
        Self {
            default_sorting_column: "id",
            default_sorted: SortDirection::Desc,
            default_export_ignore_columns: &["actions", "action"],
            default_timestamp_columns: &["created_at", "updated_at", "deleted_at"],
            default_unsortable: &[],
            default_row_per_page: None,
        }
    }
}
pub trait AdminDataTableHooks: Send + Sync + 'static {
    fn scope<'db>(&'db self, query: Query<'db, AdminModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> Query<'db, AdminModel> { query }
    fn authorize(&self, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<bool> { Ok(true) }
    fn filter_query<'db>(&'db self, _query: Query<'db, AdminModel>, _filter_key: &str, _value: &str, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<Option<Query<'db, AdminModel>>> { Ok(None) }
    fn filters<'db>(&'db self, query: Query<'db, AdminModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<Query<'db, AdminModel>> { Ok(query) }
    fn map_row(&self, _row: &mut AdminRecord, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<()> { Ok(()) }
    fn default_row_to_record(&self, row: AdminRecord) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> {
        let value = serde_json::to_value(&row)?;
        let mut record = match value { serde_json::Value::Object(map) => map, _ => anyhow::bail!("Generated row must serialize to a JSON object"), };
        if let Some(id_value) = record.get("id").cloned() {
            let id_text = match id_value {
                serde_json::Value::Number(number) => number.to_string(),
                serde_json::Value::String(text) => text,
                other => other.to_string(),
            };
            record.insert("id".to_string(), serde_json::Value::String(id_text));
        }
        record.insert("identity".to_string(), serde_json::to_value(row.identity())?);
        Ok(record)
    }
    fn row_to_record(&self, row: AdminRecord, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> {
        self.default_row_to_record(row)
    }
    fn summary<'db>(&'db self, _query: Query<'db, AdminModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> BoxFuture<'db, anyhow::Result<Option<serde_json::Value>>> { Box::pin(async { Ok(None) }) }
}
#[derive(Default)]
pub struct AdminDefaultDataTableHooks;
impl AdminDataTableHooks for AdminDefaultDataTableHooks {}
pub struct AdminDataTable<H = AdminDefaultDataTableHooks> where H: AdminDataTableHooks {
    pub db: sqlx::PgPool,
    pub hooks: H,
    pub config: AdminDataTableConfig,
    adapter: AdminTableAdapter,
}
impl AdminDataTable<AdminDefaultDataTableHooks> {
    pub fn new(db: sqlx::PgPool) -> Self {
        Self {
            db,
            hooks: AdminDefaultDataTableHooks,
            config: AdminDataTableConfig::default(),
            adapter: AdminTableAdapter,
        }
    }
}
impl<H: AdminDataTableHooks> AdminDataTable<H> {
    pub fn with_hooks<NH: AdminDataTableHooks>(self, hooks: NH) -> AdminDataTable<NH> {
        AdminDataTable {
            db: self.db,
            hooks,
            config: self.config,
            adapter: AdminTableAdapter,
        }
    }
    pub fn with_config(mut self, config: AdminDataTableConfig) -> Self {
        self.config = config;
        self
    }
}
impl<H: AdminDataTableHooks> AutoDataTable for AdminDataTable<H> {
    type Adapter = AdminTableAdapter;
    fn adapter(&self) -> &Self::Adapter { &self.adapter }
    fn base_query<'db>(&'db self, input: &DataTableInput, ctx: &DataTableContext) -> Query<'db, AdminModel> {
        self.hooks.scope(AdminModel::query(&self.db), input, ctx)
    }
    fn authorize(&self, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<bool> { self.hooks.authorize(input, ctx) }
    fn filter_query<'db>(&'db self, query: Query<'db, AdminModel>, filter_key: &str, value: &str, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<Option<Query<'db, AdminModel>>> { self.hooks.filter_query(query, filter_key, value, input, ctx) }
    fn filters<'db>(&'db self, query: Query<'db, AdminModel>, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<Query<'db, AdminModel>> { self.hooks.filters(query, input, ctx) }
    fn map_row(&self, row: &mut AdminRecord, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<()> { self.hooks.map_row(row, input, ctx) }
    fn row_to_record(&self, row: AdminRecord, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> { self.hooks.row_to_record(row, input, ctx) }
    fn summary<'db>(&'db self, query: Query<'db, AdminModel>, input: &DataTableInput, ctx: &DataTableContext) -> BoxFuture<'db, anyhow::Result<Option<serde_json::Value>>> where Self: 'db { self.hooks.summary(query, input, ctx) }
    fn default_sorting_column(&self) -> &'static str { self.config.default_sorting_column }
    fn default_sorted(&self) -> SortDirection { self.config.default_sorted }
    fn default_export_ignore_columns(&self) -> &'static [&'static str] { self.config.default_export_ignore_columns }
    fn default_timestamp_columns(&self) -> &'static [&'static str] { self.config.default_timestamp_columns }
    fn default_unsortable(&self) -> &'static [&'static str] { self.config.default_unsortable }
    fn default_row_per_page(&self, ctx: &DataTableContext) -> i64 { self.config.default_row_per_page.unwrap_or(ctx.default_per_page) }
}
use core_db::common::active_record::ActiveRecord;
#[async_trait::async_trait]
impl ActiveRecord for AdminRecord {
    type Id = i64;
    async fn find(db: &sqlx::PgPool, id: Self::Id) -> anyhow::Result<Option<Self>> {
        AdminModel::find(db, id).await.map_err(|e| e.into())
    }
}
pub struct AdminModel;
impl AdminModel {
    pub const TABLE: &'static str = "admin";
    pub const MODEL_KEY: &'static str = "admin";
    pub const PK: &'static str = "id";
    pub fn query<'db>(db: impl Into<DbConn<'db>>) -> Query<'db, AdminModel> {
        Query::new(db)
    }
    pub fn query_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Query<'db, AdminModel> {
        Query::new_with_base_url(db, base_url)
    }
    pub fn create<'db>(db: impl Into<DbConn<'db>>) -> Create<'db, AdminModel> {
        Create::new(db)
    }
    pub fn create_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Create<'db, AdminModel> {
        Create::new_with_base_url(db, base_url)
    }
    pub fn patch<'db>(db: impl Into<DbConn<'db>>) -> Patch<'db, AdminModel> {
        Patch::new(db)
    }
    pub fn patch_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Patch<'db, AdminModel> {
        Patch::new_with_base_url(db, base_url)
    }
    pub async fn find<'db>(db: impl Into<DbConn<'db>>, id: i64) -> Result<Option<AdminRecord>> {
        Query::<AdminModel>::new(db).find(id).await
    }
    fn _transform_create_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        match col {
            c if c == AdminDbCol::Password.as_sql() => {
                let BindValue::String(value) = value else {
                    anyhow::bail!("column '{}' expects String before hashing, got '{:?}'", col, value);
                };
                let hashed = core_db::common::auth::hash::hash_password(&value)?;
                Ok(hashed.into())
            }
            _ => Ok(value),
        }
    }
    fn _transform_patch_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        match col {
            c if c == AdminDbCol::Password.as_sql() => {
                let BindValue::String(value) = value else {
                    anyhow::bail!("column '{}' expects String before hashing, got '{:?}'", col, value);
                };
                let hashed = core_db::common::auth::hash::hash_password(&value)?;
                Ok(hashed.into())
            }
            _ => Ok(value),
        }
    }
}

impl ModelDef for AdminModel {
    type Pk = i64;
    type Record = AdminRecord;
    type Create = AdminCreate;
    type Changes = AdminChanges;
    const TABLE: &'static str = AdminModel::TABLE;
    const MODEL_KEY: &'static str = AdminModel::MODEL_KEY;
    const PK_COL: &'static str = "id";
}

impl core_db::common::model_api::QueryModel for AdminModel {
    const DEFAULT_SELECT: &'static str = "id, username, email, locale, password, name, admin_type, abilities, created_at, updated_at, deleted_at";
    const HAS_SOFT_DELETE: bool = true;
    const SOFT_DELETE_COL: &'static str = "deleted_at";
    const HAS_CREATED_AT: bool = true;
    const HAS_UPDATED_AT: bool = true;
    fn query_all<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Vec<Self::Record>> {
        Box::pin(async move {
            let (sql, binds) = state.to_select_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_as::<_, AdminRow>(&sql);
            for b in binds { q = bind(q, b); }
            let rows = state.db.fetch_all(q).await?;
            record_profiled_query("admin", "SELECT", &sql, &__profiler_binds, __profiler_start.elapsed());
            let ids: Vec<i64> = rows.iter().map(|r| r.id.clone()).collect();
            let localized = LocalizedMap::default();
            let mut out_vec = Vec::with_capacity(rows.len());
            for r in rows {
                out_vec.push(hydrate_record(r, &LocalizedMap::default(), state.base_url.as_deref()));
            }
            let out_vec: Vec<AdminRecord> = out_vec;
            Ok(out_vec)
        })
    }
    fn query_first<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Option<Self::Record>> {
        Box::pin(async move {
            let mut v = Self::query_all(state.limit(1)).await?;
            Ok(v.pop())
        })
    }
    fn query_find<'db>(state: QueryState<'db>, id: Self::Pk) -> core_db::common::model_api::BoxModelFuture<'db, Option<Self::Record>> {
        Box::pin(async move { Self::query_first(state.where_col_str("id", Op::Eq, id.into())).await })
    }
    fn query_count<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, i64> {
        Box::pin(async move {
            let (sql, binds) = state.to_count_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_scalar::<_, i64>(&sql);
            for b in binds { q = bind_scalar(q, b); }
            let count = state.db.fetch_scalar(q).await?;
            record_profiled_query("admin", "COUNT", &sql, &__profiler_binds, __profiler_start.elapsed());
            Ok(count)
        })
    }
    fn query_delete<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, u64> {
        Box::pin(async move {
            if state.limit.is_some() {
                anyhow::bail!("delete() does not support limit; add where clauses");
            }
            let db = state.db;
            let mut where_sql = state.where_sql;
            let binds = state.binds;
            let with_deleted = state.with_deleted;
            let only_deleted = state.only_deleted;
            if where_sql.is_empty() { anyhow::bail!("delete(): no conditions set"); }
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __observer_active = try_get_observer().is_some();
            let __old_rows: Vec<AdminRow> = if __observer_active {
                let select_sql = format!("SELECT * FROM admin WHERE {}", where_sql.join(" AND "));
                let mut fq = sqlx::query_as::<_, AdminRow>(&select_sql);
                for b in &binds { fq = bind(fq, b.clone()); }
                let rows: Vec<AdminRow> = db.fetch_all(fq).await.unwrap_or_default();
                rows
            } else {
                Vec::new()
            };
            if !__old_rows.is_empty() {
                if let Some(observer) = try_get_observer() {
                    for old_row in &__old_rows {
                        let old_data = serde_json::to_value(old_row)?;
                        let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", old_row.id)) };
                        observer.on_deleting(&event, &old_data).await?;
                    }
                }
            }
            if HAS_SOFT_DELETE {
                if only_deleted {
                    where_sql.push(format!("{} IS NOT NULL", AdminDbCol::DeletedAt.as_sql()));
                } else if !with_deleted {
                    where_sql.push(format!("{} IS NULL", AdminDbCol::DeletedAt.as_sql()));
                }
                let idx = binds.len() + 1;
                let mut sql = format!("UPDATE admin SET {} = ${}", AdminDbCol::DeletedAt.as_sql(), idx);
                if !where_sql.is_empty() {
                    sql.push_str(" WHERE ");
                    sql.push_str(&where_sql.join(" AND "));
                }
                let __profiler_start = std::time::Instant::now();
                let mut q = sqlx::query(&sql);
                for b in binds { q = bind_query(q, b); }
                q = bind_query(q, time::OffsetDateTime::now_utc().into());
                let res = db.execute(q).await?;
                record_profiled_query("admin", "UPDATE", &sql, &__profiler_binds, __profiler_start.elapsed());
                if !__old_rows.is_empty() && res.rows_affected() > 0 {
                    if let Some(observer) = try_get_observer() {
                        for old_row in &__old_rows {
                            let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", old_row.id)) };
                            match serde_json::to_value(old_row) {
                                Ok(old_data) => {
                                    if let Err(err) = observer.on_deleted(&event, &old_data).await {
                                        log_observer_error("deleted", "admin", &err);
                                    }
                                }
                                Err(err) => log_observer_error("deleted", "admin", &err),
                            }
                        }
                    }
                }
                return Ok(res.rows_affected());
            }
            let mut sql = String::from("DELETE FROM admin");
            if !where_sql.is_empty() {
                sql.push_str(" WHERE ");
                sql.push_str(&where_sql.join(" AND "));
            }
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query(&sql);
            for b in binds { q = bind_query(q, b); }
            let res = db.execute(q).await?;
            record_profiled_query("admin", "DELETE", &sql, &__profiler_binds, __profiler_start.elapsed());
            if !__old_rows.is_empty() && res.rows_affected() > 0 {
                if let Some(observer) = try_get_observer() {
                    for old_row in &__old_rows {
                        let event = ModelEvent { model: "admin", table: "admin", record_key: Some(format!("{}", old_row.id)) };
                        match serde_json::to_value(old_row) {
                            Ok(old_data) => {
                                if let Err(err) = observer.on_deleted(&event, &old_data).await {
                                    log_observer_error("deleted", "admin", &err);
                                }
                            }
                            Err(err) => log_observer_error("deleted", "admin", &err),
                        }
                    }
                }
            }
            Ok(res.rows_affected())
        })
    }
    fn query_paginate<'db>(state: QueryState<'db>, page: i64, per_page: i64) -> core_db::common::model_api::BoxModelFuture<'db, core_db::common::model_api::Page<Self::Record>> {
        Box::pin(async move {
            let page = if page < 1 { 1 } else { page };
            let per_page = resolve_per_page(per_page);
            let (count_sql, count_binds) = state.to_count_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { count_binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut count_q = sqlx::query_scalar::<_, i64>(&count_sql);
            for b in count_binds { count_q = bind_scalar(count_q, b); }
            let total: i64 = state.db.fetch_scalar(count_q).await?;
            record_profiled_query("admin", "COUNT", &count_sql, &__profiler_binds, __profiler_start.elapsed());
            let last_page = ((total + per_page - 1) / per_page).max(1);
            let current_page = page.min(last_page);
            let offset_val = (current_page - 1) * per_page;
            let mut state = state;
            state.offset = Some(offset_val);
            state.limit = Some(per_page);
            let (sql, binds) = state.to_select_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_as::<_, AdminRow>(&sql);
            for b in binds { q = bind(q, b); }
            let rows = state.db.fetch_all(q).await?;
            record_profiled_query("admin", "SELECT", &sql, &__profiler_binds, __profiler_start.elapsed());
            let ids: Vec<i64> = rows.iter().map(|r| r.id.clone()).collect();
            let localized = LocalizedMap::default();
            let mut data = Vec::with_capacity(rows.len());
            for r in rows {
                data.push(hydrate_record(r, &LocalizedMap::default(), state.base_url.as_deref()));
            }
            let data: Vec<AdminRecord> = data;
            Ok(core_db::common::model_api::Page { data, total, per_page, current_page, last_page })
        })
    }
}

impl core_db::common::model_api::CreateModel for AdminModel {
    fn create_save<'db>(state: CreateState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Self::Record> {
        Box::pin(async move {
            let builder = AdminCreateInner::from_state(state);
            let db = builder.state.db.clone();
            let base_url = builder.state.base_url.clone();
            let created = builder.save().await?;
            Query::<AdminModel>::new_with_base_url(db, base_url).find(created.id.clone()).await?.ok_or_else(|| anyhow::anyhow!("admin: created record not found"))
        })
    }
    fn transform_create_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        Self::_transform_create_value(col, value)
    }
}

impl core_db::common::model_api::CreateField<AdminModel> for AdminDbCol {
    type Value = BindValue;
    fn set<'db>(field: Self, state: CreateState<'db>, value: BindValue) -> anyhow::Result<CreateState<'db>> {
        let value = <AdminModel as core_db::common::model_api::CreateModel>::transform_create_value(field.as_sql(), value)?;
        Ok(state.set_col(field.as_sql(), value))
    }
}

impl core_db::common::model_api::CreateConflictField<AdminModel> for AdminDbCol {
    fn on_conflict_do_nothing<'db>(state: CreateState<'db>, fields: &[Self]) -> CreateState<'db> {
        let cols: Vec<&'static str> = fields.iter().map(|f| f.as_sql()).collect();
        state.on_conflict_do_nothing(&cols)
    }
    fn on_conflict_update<'db>(state: CreateState<'db>, fields: &[Self]) -> CreateState<'db> {
        let cols: Vec<&'static str> = fields.iter().map(|f| f.as_sql()).collect();
        state.on_conflict_update(&cols)
    }
}

impl core_db::common::model_api::PatchModel for AdminModel {
    fn patch_from_query<'db>(mut state: QueryState<'db>) -> PatchState<'db> {
        let db = state.db.clone();
        let base_url = state.base_url.clone();
        state.select_sql = Some(AdminDbCol::Id.as_sql().to_string());
        let (scope_sql, binds) = state.to_sql();
        let mut ps = PatchState::new(db, base_url, "admin");
        ps.where_sql.push(format!("{} IN ({})", AdminDbCol::Id.as_sql(), scope_sql));
        ps.where_binds = binds;
        ps
    }
    fn patch_save<'db>(state: PatchState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, u64> {
        Box::pin(async move {
            let builder = AdminPatchInner::from_state(state);
            builder.save().await
        })
    }
    fn patch_fetch<'db>(state: PatchState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Vec<Self::Record>> {
        Box::pin(async move {
            if state.where_sql.is_empty() {
                anyhow::bail!("update: no conditions set");
            }
            let db = state.db.clone();
            let base_url = state.base_url.clone();
            let mut select_sql = format!("SELECT {} FROM admin", AdminDbCol::Id.as_sql());
            select_sql.push_str(&format!(" WHERE {}", state.where_sql.join(" AND ")));
            let mut select_q = sqlx::query_scalar::<_, i64>(&select_sql);
            for bind_value in &state.where_binds { select_q = bind_scalar(select_q, bind_value.clone()); }
            let target_ids = db.fetch_all_scalar(select_q).await?;
            let builder = AdminPatchInner::from_state(state);
            builder.save().await?;
            if target_ids.is_empty() {
                return Ok(Vec::new());
            }
            let query = Query::<AdminModel>::new_with_base_url(db, base_url);
            let mut state = query.into_inner().with_deleted();
            let binds: Vec<BindValue> = target_ids.iter().cloned().map(Into::into).collect();
            state = state.where_in_str(AdminDbCol::Id.as_sql(), &binds);
            <Self as core_db::common::model_api::QueryModel>::query_all(state).await
        })
    }
    fn transform_patch_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        Self::_transform_patch_value(col, value)
    }
}

impl core_db::common::model_api::PatchAssignField<AdminModel> for AdminDbCol {
    type Value = BindValue;
    fn assign<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        let value = <AdminModel as core_db::common::model_api::PatchModel>::transform_patch_value(field.as_sql(), value)?;
        Ok(state.assign_col(field.as_sql(), value))
    }
}

impl core_db::common::model_api::PatchNumericField<AdminModel> for AdminDbCol {
    fn increment<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        match field {
            AdminDbCol::Id => Ok(state.increment_col(field.as_sql(), value)),
            _ => anyhow::bail!("column '{}' does not support increment", field.as_sql()),
        }
    }
    fn decrement<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        match field {
            AdminDbCol::Id => Ok(state.decrement_col(field.as_sql(), value)),
            _ => anyhow::bail!("column '{}' does not support decrement", field.as_sql()),
        }
    }
}

impl core_db::common::model_api::ColExpr for AdminDbCol {
    fn col_sql(self) -> &'static str { self.as_sql() }
}

impl core_db::common::model_api::QueryField<AdminModel> for AdminDbCol {
    type Value = BindValue;
    fn where_col<'db>(field: Self, state: QueryState<'db>, op: Op, value: BindValue) -> QueryState<'db> {
        state.where_col_str(field.as_sql(), op, value)
    }
    fn or_where_col<'db>(field: Self, state: QueryState<'db>, op: Op, value: BindValue) -> QueryState<'db> {
        state.or_where_col_str(field.as_sql(), op, value)
    }
    fn where_in<'db>(field: Self, state: QueryState<'db>, values: &[BindValue]) -> QueryState<'db> {
        state.where_in_str(field.as_sql(), values)
    }
    fn order_by<'db>(field: Self, state: QueryState<'db>, dir: OrderDir) -> QueryState<'db> {
        state.order_by_str(field.as_sql(), dir)
    }
    fn where_null<'db>(field: Self, state: QueryState<'db>) -> QueryState<'db> {
        state.where_null_str(field.as_sql())
    }
    fn where_not_null<'db>(field: Self, state: QueryState<'db>) -> QueryState<'db> {
        state.where_not_null_str(field.as_sql())
    }
}


