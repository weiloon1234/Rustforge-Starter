// AUTO-GENERATED FILE — DO NOT EDIT
// Generated by build.rs from Rust model sources + configs.toml
#![allow(dead_code, unused_imports, unused_variables, unused_mut)]
use anyhow::Result;
use std::collections::HashMap;
use serde::{Deserialize, Serialize};
use schemars::JsonSchema;
use sqlx::FromRow;
use core_db::common::sql::{BindValue, Op, OrderDir, SetMode, bind, bind_query, bind_scalar, generate_snowflake_i64, is_sql_profiler_enabled, format_duration, record_profiled_query, DbConn};
use core_db::common::pagination::resolve_per_page;
use core_datatable::{AutoDataTable, BoxFuture, DataTableColumnDescriptor, DataTableContext, DataTableInput, DataTableRelationColumnDescriptor, GeneratedTableAdapter, ParsedFilter, SortDirection};
use core_db::platform::localized::types::LocalizedMap;
use crate::generated::models::common::{FieldChange, FieldInput, Page, log_observer_error, renumber_placeholders};
use core_db::common::model_api::{ColExpr, Column, Create, CreateState, ManyRelation, ModelDef, OneRelation, Patch, PatchState, Query, QueryState};
use crate::generated::models::crypto_network::{CryptoNetworkDbCol, CryptoNetworkModel, CryptoNetworkRow};
use super::enums::*;
use core_db::common::model_observer::{ModelEvent, try_get_observer};
const HAS_CREATED_AT: bool = true;
const HAS_UPDATED_AT: bool = true;
const HAS_SOFT_DELETE: bool = false;
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[doc(hidden)]
pub struct CompanyCryptoAccountCreate {
    pub id: FieldInput<i64>,
    pub crypto_network_id: FieldInput<i64>,
    pub wallet_address: FieldInput<String>,
    pub conversion_rate: FieldInput<rust_decimal::Decimal>,
    pub status: FieldInput<CompanyCryptoAccountStatus>,
    pub sort_order: FieldInput<i32>,
    pub created_at: FieldInput<time::OffsetDateTime>,
    pub updated_at: FieldInput<time::OffsetDateTime>,
}

#[derive(Debug, Clone, Default, Serialize, Deserialize)]
#[doc(hidden)]
pub struct CompanyCryptoAccountChanges {
    pub id: Option<FieldChange<i64>>,
    pub crypto_network_id: Option<FieldChange<i64>>,
    pub wallet_address: Option<FieldChange<String>>,
    pub conversion_rate: Option<FieldChange<rust_decimal::Decimal>>,
    pub status: Option<FieldChange<CompanyCryptoAccountStatus>>,
    pub sort_order: Option<FieldChange<i32>>,
    pub created_at: Option<FieldChange<time::OffsetDateTime>>,
    pub updated_at: Option<FieldChange<time::OffsetDateTime>>,
}

#[derive(Debug, Clone, FromRow, Serialize, Deserialize, JsonSchema)]
#[doc(hidden)]
pub struct CompanyCryptoAccountRow {
    pub id: i64,
    pub crypto_network_id: i64,
    pub wallet_address: String,
    pub conversion_rate: rust_decimal::Decimal,
    pub status: CompanyCryptoAccountStatus,
    pub sort_order: i32,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub created_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub updated_at: time::OffsetDateTime,
}

#[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
pub struct CompanyCryptoAccountRecord {
    pub id: i64,
    pub crypto_network_id: i64,
    pub wallet_address: String,
    pub conversion_rate: rust_decimal::Decimal,
    pub status: CompanyCryptoAccountStatus,
    pub sort_order: i32,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub created_at: time::OffsetDateTime,
    #[serde(with = "time::serde::rfc3339")]
    #[schemars(with = "String")]
    pub updated_at: time::OffsetDateTime,
    #[serde(default)]
    pub status_explained: String,
    #[serde(default)]
    pub crypto_network: Option<CryptoNetworkRow>,
}

impl CompanyCryptoAccountRecord {
    pub fn update<'db>(&self, db: impl Into<DbConn<'db>>) -> Patch<'db, CompanyCryptoAccountModel> {
        CompanyCryptoAccountModel::query(db.into()).where_col(CompanyCryptoAccountDbCol::Id, Op::Eq, self.id.clone()).patch()
    }
}

fn hydrate_record(row: CompanyCryptoAccountRow, _loc: &LocalizedMap, _base_url: Option<&str>) -> CompanyCryptoAccountRecord {
    let mut record = CompanyCryptoAccountRecord {
        id: row.id,
        crypto_network_id: row.crypto_network_id,
        wallet_address: row.wallet_address,
        conversion_rate: row.conversion_rate,
        status: row.status,
        sort_order: row.sort_order,
        created_at: row.created_at,
        updated_at: row.updated_at,
        status_explained: row.status.explained_label(),
        crypto_network: None,
    };
    record
}

impl CompanyCryptoAccountRecord {
    pub fn one<R>(&self, relation: R) -> Option<&R::Target>
    where
        R: core_db::common::model_api::RecordOneRelation<CompanyCryptoAccountModel>,
    {
        R::get(relation, self)
    }
    pub fn many<R>(&self, relation: R) -> &[R::Target]
    where
        R: core_db::common::model_api::RecordManyRelation<CompanyCryptoAccountModel>,
    {
        R::get(relation, self)
    }
}

#[derive(Debug, Clone, Copy, Default)]
pub struct CompanyCryptoAccountCol;
impl CompanyCryptoAccountCol {
    pub const ID: Column<CompanyCryptoAccountModel, i64> = Column::new("id");
    pub const CRYPTO_NETWORK_ID: Column<CompanyCryptoAccountModel, i64> = Column::new("crypto_network_id");
    pub const WALLET_ADDRESS: Column<CompanyCryptoAccountModel, String> = Column::new("wallet_address");
    pub const CONVERSION_RATE: Column<CompanyCryptoAccountModel, rust_decimal::Decimal> = Column::new("conversion_rate");
    pub const STATUS: Column<CompanyCryptoAccountModel, CompanyCryptoAccountStatus> = Column::new("status");
    pub const SORT_ORDER: Column<CompanyCryptoAccountModel, i32> = Column::new("sort_order");
    pub const CREATED_AT: Column<CompanyCryptoAccountModel, time::OffsetDateTime> = Column::new("created_at");
    pub const UPDATED_AT: Column<CompanyCryptoAccountModel, time::OffsetDateTime> = Column::new("updated_at");
}

fn resolve_company_crypto_account_db_col(sql: &str) -> Option<CompanyCryptoAccountDbCol> {
    match sql {
        "id" => Some(CompanyCryptoAccountDbCol::Id),
        "crypto_network_id" => Some(CompanyCryptoAccountDbCol::CryptoNetworkId),
        "wallet_address" => Some(CompanyCryptoAccountDbCol::WalletAddress),
        "conversion_rate" => Some(CompanyCryptoAccountDbCol::ConversionRate),
        "status" => Some(CompanyCryptoAccountDbCol::Status),
        "sort_order" => Some(CompanyCryptoAccountDbCol::SortOrder),
        "created_at" => Some(CompanyCryptoAccountDbCol::CreatedAt),
        "updated_at" => Some(CompanyCryptoAccountDbCol::UpdatedAt),
        _ => None,
    }
}

#[derive(Debug, Clone, Copy, JsonSchema)]
pub enum CompanyCryptoAccountDbCol {
    Id,
    CryptoNetworkId,
    WalletAddress,
    ConversionRate,
    Status,
    SortOrder,
    CreatedAt,
    UpdatedAt,
}

impl CompanyCryptoAccountDbCol {
    pub const fn all() -> &'static [CompanyCryptoAccountDbCol] {
        &[CompanyCryptoAccountDbCol::Id, CompanyCryptoAccountDbCol::CryptoNetworkId, CompanyCryptoAccountDbCol::WalletAddress, CompanyCryptoAccountDbCol::ConversionRate, CompanyCryptoAccountDbCol::Status, CompanyCryptoAccountDbCol::SortOrder, CompanyCryptoAccountDbCol::CreatedAt, CompanyCryptoAccountDbCol::UpdatedAt]
    }
    pub const fn as_sql(self) -> &'static str {
        match self {
            CompanyCryptoAccountDbCol::Id => "id",
            CompanyCryptoAccountDbCol::CryptoNetworkId => "crypto_network_id",
            CompanyCryptoAccountDbCol::WalletAddress => "wallet_address",
            CompanyCryptoAccountDbCol::ConversionRate => "conversion_rate",
            CompanyCryptoAccountDbCol::Status => "status",
            CompanyCryptoAccountDbCol::SortOrder => "sort_order",
            CompanyCryptoAccountDbCol::CreatedAt => "created_at",
            CompanyCryptoAccountDbCol::UpdatedAt => "updated_at",
        }
    }
}

#[derive(Debug, Clone, Copy, Default)]
pub struct CompanyCryptoAccountRel;
impl CompanyCryptoAccountRel {
    pub const CRYPTO_NETWORK: OneRelation<CompanyCryptoAccountModel, CryptoNetworkRow, 0> = OneRelation::<CompanyCryptoAccountModel, CryptoNetworkRow, 0>::new("crypto_network");
}

async fn load_crypto_network<'db>(db: DbConn<'db>, parents: &[CompanyCryptoAccountRow]) -> Result<HashMap<i64, Option<CryptoNetworkRow>>> {
        if parents.is_empty() { return Ok(HashMap::new()); }
        let mut fk_vals = Vec::new();
        let mut parent_pairs = Vec::new();
        for p in parents {
            fk_vals.push(p.crypto_network_id.clone());
            parent_pairs.push((p.id.clone(), Some(p.crypto_network_id.clone())));
        }
        if fk_vals.is_empty() { return Ok(HashMap::new()); }
        let placeholders: Vec<String> = (1..=fk_vals.len()).map(|i| format!("${}", i)).collect();
        let sql = format!("SELECT * FROM crypto_networks WHERE id IN ({})", placeholders.join(", "));
        let mut q = sqlx::query_as::<_, CryptoNetworkRow>(&sql);
        for fk in fk_vals { q = bind(q, fk.into()); }
        let rows = db.fetch_all(q).await?;
        let mut by_pk: HashMap<i64, CryptoNetworkRow> = HashMap::new();
        for row in rows { by_pk.insert(row.id.clone(), row); }
        let mut out = HashMap::new();
        for (pid, fk) in parent_pairs {
            out.insert(pid, fk.and_then(|k| by_pk.get(&k).cloned()));
        }
        Ok(out)
    }

pub struct CompanyCryptoAccountCreateInner<'db> {
    pub(crate) state: core_db::common::model_api::CreateState<'db>,
}

impl<'db> CompanyCryptoAccountCreateInner<'db> {
    pub fn new(db: DbConn<'db>, base_url: Option<String>) -> Self {
        Self {
            state: core_db::common::model_api::CreateState::new(db, base_url, "company_crypto_accounts"),
        }
    }
    pub fn from_state(state: core_db::common::model_api::CreateState<'db>) -> Self {
        Self {
            state,
        }
    }


pub fn set_id(mut self, val: i64) -> Self {
        self.state = self.state.set_col("id", val.into());
        self
    }
    pub fn set_crypto_network_id(mut self, val: i64) -> Self {
        self.state = self.state.set_col("crypto_network_id", val.into());
        self
    }
    pub fn set_wallet_address(mut self, val: String) -> Self {
        self.state = self.state.set_col("wallet_address", val.into());
        self
    }
    pub fn set_conversion_rate(mut self, val: rust_decimal::Decimal) -> Self {
        self.state = self.state.set_col("conversion_rate", val.into());
        self
    }
    pub fn set_status(mut self, val: CompanyCryptoAccountStatus) -> Self {
        self.state = self.state.set_col("status", val.into());
        self
    }
    pub fn set_sort_order(mut self, val: i32) -> Self {
        self.state = self.state.set_col("sort_order", val.into());
        self
    }
    pub fn set_created_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.set_col("created_at", val.into());
        self
    }
    pub fn set_updated_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.set_col("updated_at", val.into());
        self
    }
    pub fn on_conflict_do_nothing(mut self, conflict_cols: &[CompanyCryptoAccountDbCol]) -> Self {
        self.state = self.state.on_conflict_do_nothing(&conflict_cols.iter().map(|c| c.as_sql()).collect::<Vec<_>>());
        self
    }
    pub fn on_conflict_update(mut self, conflict_cols: &[CompanyCryptoAccountDbCol]) -> Self {
        self.state = self.state.on_conflict_update(&conflict_cols.iter().map(|c| c.as_sql()).collect::<Vec<_>>());
        self
    }
    fn to_create_input(&self) -> Result<CompanyCryptoAccountCreate> {
        let mut input = CompanyCryptoAccountCreate::default();
        for (col_name, bind) in self.state.col_names.iter().zip(self.state.binds.iter()) {
            match *col_name {
                "id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    input.id = FieldInput::Set(value);
                }
                "crypto_network_id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    input.crypto_network_id = FieldInput::Set(value);
                }
                "wallet_address" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    input.wallet_address = FieldInput::Set(value);
                }
                "conversion_rate" => {
                    let value = match bind {
            BindValue::Decimal(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'rust_decimal::Decimal'", other),
        };
                    input.conversion_rate = FieldInput::Set(value);
                }
                "status" => {
                    let value = match bind {
                BindValue::String(value) => CompanyCryptoAccountStatus::from_storage(value)
                    .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'CompanyCryptoAccountStatus'", value))?,
                BindValue::I64(value) => {
                    let raw = value.to_string();
                    CompanyCryptoAccountStatus::from_storage(&raw)
                        .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'CompanyCryptoAccountStatus'", value))?
                }
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'CompanyCryptoAccountStatus'", other),
            };
                    input.status = FieldInput::Set(value);
                }
                "sort_order" => {
                    let value = match bind {
            BindValue::I32(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i32'", other),
        };
                    input.sort_order = FieldInput::Set(value);
                }
                "created_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    input.created_at = FieldInput::Set(value);
                }
                "updated_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    input.updated_at = FieldInput::Set(value);
                }
                _ => {}
            }
        }
        Ok(input)
    }


pub async fn save(self) -> Result<CompanyCryptoAccountRecord> {
        let __create_input = if try_get_observer().is_some() {
            Some(self.to_create_input()?)
        } else {
            None
        };
        if let Some(observer) = try_get_observer() {
            if let Some(create_input) = __create_input.as_ref() {
                let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: None };
                let data = serde_json::to_value(create_input)?;
                observer.on_creating(&event, &data).await?;
            }
        }
        let db_conn = self.state.db.clone();
        match db_conn {
            DbConn::Pool(pool) => {
                let tx = pool.begin().await?;
                let tx_lock = std::sync::Arc::new(tokio::sync::Mutex::new(tx));
                let (record, row) = {
                    let db = DbConn::tx(tx_lock.clone());
                    self.save_with_db(db).await?
                };
                let tx = std::sync::Arc::try_unwrap(tx_lock)
                    .map_err(|_| anyhow::anyhow!("transaction scope still has active handles"))?
                    .into_inner();
                tx.commit().await?;
                if let Some(observer) = try_get_observer() {
                    let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", row.id)) };
                    match serde_json::to_value(&row) {
                        Ok(data) => {
                            if let Err(err) = observer.on_created(&event, &data).await {
                                log_observer_error("created", "company_crypto_account", &err);
                            }
                        }
                        Err(err) => log_observer_error("created", "company_crypto_account", &err),
                    }
                }
                Ok(record)
            }
            DbConn::Tx(_) => {
                let (record, row) = self.save_with_db(db_conn).await?;
                if let Some(observer) = try_get_observer() {
                    let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", row.id)) };
                    match serde_json::to_value(&row) {
                        Ok(data) => {
                            if let Err(err) = observer.on_created(&event, &data).await {
                                log_observer_error("created", "company_crypto_account", &err);
                            }
                        }
                        Err(err) => log_observer_error("created", "company_crypto_account", &err),
                    }
                }
                Ok(record)
            }
        }
    }

    async fn save_with_db<'tx>(mut self, db: DbConn<'tx>) -> Result<(CompanyCryptoAccountRecord, CompanyCryptoAccountRow)> {
        if !self.state.col_names.contains(&"id") {
            self.state = self.state.set_col("id", generate_snowflake_i64().into());
        }
        if HAS_CREATED_AT && !self.state.col_names.contains(&"created_at") {
            self.state = self.state.set_col("created_at", time::OffsetDateTime::now_utc().into());
        }
        if HAS_UPDATED_AT && !self.state.col_names.contains(&"updated_at") {
            self.state = self.state.set_col("updated_at", time::OffsetDateTime::now_utc().into());
        }
        if self.state.col_names.is_empty() {
            anyhow::bail!("insert: no columns set");
        }
        let base_url = self.state.base_url.clone();
        let (sql, binds) = self.state.build_insert_sql();
        let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
        let __profiler_start = std::time::Instant::now();
        let mut q = sqlx::query_as::<_, CompanyCryptoAccountRow>(&sql);
        for b in binds {
            q = bind(q, b);
        }
        let row = db.fetch_one(q).await?;
        record_profiled_query("company_crypto_accounts", "INSERT", &sql, &__profiler_binds, __profiler_start.elapsed());
        let localized = LocalizedMap::default();
        let record = hydrate_record(row.clone(), &LocalizedMap::default(), base_url.as_deref());
        Ok((record, row))
    }
}

pub struct CompanyCryptoAccountPatchInner<'db> {
    pub(crate) state: core_db::common::model_api::PatchState<'db>,
}

impl<'db> CompanyCryptoAccountPatchInner<'db> {
    pub fn new(db: DbConn<'db>, base_url: Option<String>) -> Self {
        Self {
            state: core_db::common::model_api::PatchState::new(db, base_url, "company_crypto_accounts"),
        }
    }
    pub fn from_state(state: core_db::common::model_api::PatchState<'db>) -> Self {
        Self {
            state,
        }
    }


pub fn set_id(mut self, val: i64) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn increment_id(mut self, val: i64) -> Self {
        self.state = self.state.increment_col(CompanyCryptoAccountDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn decrement_id(mut self, val: i64) -> Self {
        self.state = self.state.decrement_col(CompanyCryptoAccountDbCol::Id.as_sql(), val.into());
        self
    }
    pub fn set_crypto_network_id(mut self, val: i64) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::CryptoNetworkId.as_sql(), val.into());
        self
    }
    pub fn increment_crypto_network_id(mut self, val: i64) -> Self {
        self.state = self.state.increment_col(CompanyCryptoAccountDbCol::CryptoNetworkId.as_sql(), val.into());
        self
    }
    pub fn decrement_crypto_network_id(mut self, val: i64) -> Self {
        self.state = self.state.decrement_col(CompanyCryptoAccountDbCol::CryptoNetworkId.as_sql(), val.into());
        self
    }
    pub fn set_wallet_address(mut self, val: String) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::WalletAddress.as_sql(), val.into());
        self
    }
    pub fn set_conversion_rate(mut self, val: rust_decimal::Decimal) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::ConversionRate.as_sql(), val.into());
        self
    }
    pub fn increment_conversion_rate(mut self, val: rust_decimal::Decimal) -> Self {
        self.state = self.state.increment_col(CompanyCryptoAccountDbCol::ConversionRate.as_sql(), val.into());
        self
    }
    pub fn decrement_conversion_rate(mut self, val: rust_decimal::Decimal) -> Self {
        self.state = self.state.decrement_col(CompanyCryptoAccountDbCol::ConversionRate.as_sql(), val.into());
        self
    }
    pub fn set_status(mut self, val: CompanyCryptoAccountStatus) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::Status.as_sql(), val.into());
        self
    }
    pub fn set_sort_order(mut self, val: i32) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::SortOrder.as_sql(), val.into());
        self
    }
    pub fn increment_sort_order(mut self, val: i32) -> Self {
        self.state = self.state.increment_col(CompanyCryptoAccountDbCol::SortOrder.as_sql(), val.into());
        self
    }
    pub fn decrement_sort_order(mut self, val: i32) -> Self {
        self.state = self.state.decrement_col(CompanyCryptoAccountDbCol::SortOrder.as_sql(), val.into());
        self
    }
    pub fn set_created_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::CreatedAt.as_sql(), val.into());
        self
    }
    pub fn set_updated_at(mut self, val: time::OffsetDateTime) -> Self {
        self.state = self.state.assign_col(CompanyCryptoAccountDbCol::UpdatedAt.as_sql(), val.into());
        self
    }
    pub fn where_id(mut self, op: Op, val: i64) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::Id.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_crypto_network_id(mut self, op: Op, val: i64) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::CryptoNetworkId.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_wallet_address(mut self, op: Op, val: String) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::WalletAddress.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_conversion_rate(mut self, op: Op, val: rust_decimal::Decimal) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::ConversionRate.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_status(mut self, op: Op, val: CompanyCryptoAccountStatus) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::Status.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_sort_order(mut self, op: Op, val: i32) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::SortOrder.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_created_at(mut self, op: Op, val: time::OffsetDateTime) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::CreatedAt.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_updated_at(mut self, op: Op, val: time::OffsetDateTime) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", CompanyCryptoAccountDbCol::UpdatedAt.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    pub fn where_col<T: Into<BindValue>>(mut self, col: CompanyCryptoAccountDbCol, op: Op, val: T) -> Self {
        let idx = self.state.where_binds.len() + 1;
        self.state.where_sql.push(format!("{} {} ${}", col.as_sql(), op.as_sql(), idx));
        self.state.where_binds.push(val.into());
        self
    }
    fn where_raw<T: Into<BindValue>>(mut self, clause: impl Into<String>, binds: impl IntoIterator<Item = T>) -> Self {
        let mut clause = clause.into();
        let incoming: Vec<BindValue> = binds.into_iter().map(Into::into).collect();
        let mut idx = self.state.where_binds.len() + 1;
        while let Some(pos) = clause.find('?') {
            let ph = format!("${}", idx);
            clause.replace_range(pos..pos + 1, &ph);
            idx += 1;
        }
        self.state.where_sql.push(clause);
        self.state.where_binds.extend(incoming);
        self
    }
    fn to_update_changes(&self) -> Result<CompanyCryptoAccountChanges> {
        let mut changes = CompanyCryptoAccountChanges::default();
        for ((col, bind), mode) in self.state.set_cols.iter().zip(self.state.set_binds.iter()).zip(self.state.set_modes.iter()) {
            match *col {
                "id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    changes.id = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "crypto_network_id" => {
                    let value = match bind {
            BindValue::I64(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i64'", other),
        };
                    changes.crypto_network_id = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "wallet_address" => {
                    let value = match bind {
            BindValue::String(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'String'", other),
        };
                    changes.wallet_address = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "conversion_rate" => {
                    let value = match bind {
            BindValue::Decimal(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'rust_decimal::Decimal'", other),
        };
                    changes.conversion_rate = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "status" => {
                    let value = match bind {
                BindValue::String(value) => CompanyCryptoAccountStatus::from_storage(value)
                    .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'CompanyCryptoAccountStatus'", value))?,
                BindValue::I64(value) => {
                    let raw = value.to_string();
                    CompanyCryptoAccountStatus::from_storage(&raw)
                        .ok_or_else(|| anyhow::anyhow!("invalid enum storage '{}' for type 'CompanyCryptoAccountStatus'", value))?
                }
                other => anyhow::bail!("unexpected bind value '{:?}' for type 'CompanyCryptoAccountStatus'", other),
            };
                    changes.status = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "sort_order" => {
                    let value = match bind {
            BindValue::I32(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'i32'", other),
        };
                    changes.sort_order = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "created_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    changes.created_at = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                "updated_at" => {
                    let value = match bind {
            BindValue::Time(value) => value.clone(),
            other => anyhow::bail!("unexpected bind value '{:?}' for type 'time::OffsetDateTime'", other),
        };
                    changes.updated_at = Some(match mode {
                        SetMode::Assign => FieldChange::Assign(value),
                        SetMode::Increment => FieldChange::Increment(value),
                        SetMode::Decrement => FieldChange::Decrement(value),
                    });
                }
                _ => {}
            }
        }
        Ok(changes)
    }


pub async fn save(self) -> Result<u64> {
        if self.state.set_cols.is_empty() { anyhow::bail!("update: no columns set"); }
        if self.state.where_sql.is_empty() { anyhow::bail!("update: no conditions set"); }
        let observer_changes = if try_get_observer().is_some() {
            Some(self.to_update_changes()?)
        } else {
            None
        };
        let db_conn = self.state.db.clone();
        match db_conn {
            DbConn::Pool(pool) => {
                let tx = pool.begin().await?;
                let tx_lock = std::sync::Arc::new(tokio::sync::Mutex::new(tx));
                let affected = {
                    let db = DbConn::tx(tx_lock.clone());
                    self.save_with_db(db, observer_changes.clone()).await?
                };
                let tx = std::sync::Arc::try_unwrap(tx_lock)
                    .map_err(|_| anyhow::anyhow!("transaction scope still has active handles"))?
                    .into_inner();
                tx.commit().await?;
                Ok(affected)
            }
            DbConn::Tx(_) => self.save_with_db(db_conn, observer_changes).await,
        }
    }

    async fn save_with_db<'tx>(self, db: DbConn<'tx>, observer_changes: Option<CompanyCryptoAccountChanges>) -> Result<u64> {
        let mut state = self.state;
        if HAS_UPDATED_AT && !state.set_cols.contains(&CompanyCryptoAccountDbCol::UpdatedAt.as_sql()) {
            let now = time::OffsetDateTime::now_utc();
            state = state.assign_col(CompanyCryptoAccountDbCol::UpdatedAt.as_sql(), now.into());
        }
        // find target ids for localized updates
        let select_sql = format!("SELECT id FROM company_crypto_accounts WHERE {}", state.where_sql.join(" AND "));
        let mut select_q = sqlx::query_scalar::<_, i64>(&select_sql);
        for b in &state.where_binds { select_q = bind_scalar(select_q, b.clone()); }
        let target_ids = db.fetch_all_scalar(select_q).await?;
        let __observer_active = try_get_observer().is_some();
        let __old_rows: Vec<CompanyCryptoAccountRow> = if __observer_active && !target_ids.is_empty() {
            let phs: Vec<String> = (1..=target_ids.len()).map(|i| format!("${}", i)).collect();
            let fetch_sql = format!("SELECT * FROM company_crypto_accounts WHERE id IN ({})", phs.join(", "));
            let mut fq = sqlx::query_as::<_, CompanyCryptoAccountRow>(&fetch_sql);
            for id in &target_ids { fq = fq.bind(id); }
            let rows: Vec<CompanyCryptoAccountRow> = db.fetch_all(fq).await.unwrap_or_default();
            rows
        } else {
            Vec::new()
        };
        if !__old_rows.is_empty() {
            if let Some(observer) = try_get_observer() {
                if let Some(changes) = observer_changes.as_ref() {
                    let changes_data = serde_json::to_value(changes)?;
                    for old_row in &__old_rows {
                        let old_data = serde_json::to_value(old_row)?;
                        let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", old_row.id)) };
                        observer.on_updating(&event, &old_data, &changes_data).await?;
                    }
                }
            }
        }
        let (sql, all_binds) = state.build_update_sql();
        let set_binds = &state.set_binds;
        let mut q = sqlx::query(&sql);
        let __profiler_binds = if is_sql_profiler_enabled() { all_binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
        let __profiler_start = std::time::Instant::now();
        for b in &all_binds { q = bind_query(q, b.clone()); }
        let res = db.execute(q).await?;
        record_profiled_query("company_crypto_accounts", "UPDATE", &sql, &__profiler_binds, __profiler_start.elapsed());
        if !__old_rows.is_empty() && res.rows_affected() > 0 {
            if let Some(observer) = try_get_observer() {
                for old_row in &__old_rows {
                    let fetch_sql = format!("SELECT * FROM company_crypto_accounts WHERE id = $1");
                    match db.fetch_optional(sqlx::query_as::<_, CompanyCryptoAccountRow>(&fetch_sql).bind(old_row.id.clone())).await {
                        Ok(Some(new_row)) => {
                            match (serde_json::to_value(old_row), serde_json::to_value(&new_row)) {
                                (Ok(old_data), Ok(new_data)) => {
                                    let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", old_row.id)) };
                                    if let Err(err) = observer.on_updated(&event, &old_data, &new_data).await {
                                        log_observer_error("updated", "company_crypto_account", &err);
                                    }
                                }
                                (Err(err), _) | (_, Err(err)) => log_observer_error("updated", "company_crypto_account", &err),
                            }
                        }
                        Ok(None) => {}
                        Err(err) => log_observer_error("updated", "company_crypto_account", &err),
                    }
                }
            }
        }
        Ok(res.rows_affected())
    }
}



pub struct CompanyCryptoAccountTableAdapter;
impl CompanyCryptoAccountTableAdapter {
    fn parse_col(name: &str) -> Option<CompanyCryptoAccountDbCol> {
        match name {
            "id" => Some(CompanyCryptoAccountDbCol::Id),
            "crypto_network_id" => Some(CompanyCryptoAccountDbCol::CryptoNetworkId),
            "wallet_address" => Some(CompanyCryptoAccountDbCol::WalletAddress),
            "conversion_rate" => Some(CompanyCryptoAccountDbCol::ConversionRate),
            "status" => Some(CompanyCryptoAccountDbCol::Status),
            "sort_order" => Some(CompanyCryptoAccountDbCol::SortOrder),
            "created_at" => Some(CompanyCryptoAccountDbCol::CreatedAt),
            "updated_at" => Some(CompanyCryptoAccountDbCol::UpdatedAt),
            _ => None,
        }
    }
    fn parse_locale_field_for_relation(relation: &str, column: &str) -> Option<&'static str> {
        match (relation, column) {
            _ => None,
        }
    }
    fn parse_locale_field(name: &str) -> Option<&'static str> {
        match name {
            _ => None,
        }
    }
    fn parse_like_col(name: &str) -> Option<CompanyCryptoAccountDbCol> {
        match name {
            "wallet_address" => Some(CompanyCryptoAccountDbCol::WalletAddress),
            _ => None,
        }
    }
    fn parse_bind_for_col(name: &str, raw: &str) -> Option<BindValue> {
        match name {
            "id" => raw.trim().parse::<i64>().ok().map(Into::into),
            "crypto_network_id" => raw.trim().parse::<i64>().ok().map(Into::into),
            "wallet_address" => Some(raw.trim().to_string().into()),
            "conversion_rate" => raw.trim().parse::<rust_decimal::Decimal>().ok().map(Into::into),
            "status" => Some(Self::parse_bind(raw.trim())),
            "sort_order" => raw.trim().parse::<i32>().ok().map(Into::into),
            "created_at" => Self::parse_datetime(raw.trim(), false).map(Into::into),
            "updated_at" => Self::parse_datetime(raw.trim(), false).map(Into::into),
            _ => None,
        }
    }
    fn parse_bind_for_relation(relation: &str, column: &str, raw: &str) -> Option<BindValue> {
        match (relation, column) {
            ("crypto_network", "id") => raw.trim().parse::<i64>().ok().map(Into::into),
            ("crypto_network", "name") => Some(raw.trim().to_string().into()),
            ("crypto_network", "symbol") => Some(raw.trim().to_string().into()),
            ("crypto_network", "status") => Some(Self::parse_bind(raw.trim())),
            ("crypto_network", "sort_order") => raw.trim().parse::<i32>().ok().map(Into::into),
            ("crypto_network", "created_at") => Self::parse_datetime(raw.trim(), false).map(Into::into),
            ("crypto_network", "updated_at") => Self::parse_datetime(raw.trim(), false).map(Into::into),
            _ => None,
        }
    }
    fn parse_bind(raw: &str) -> BindValue {
        let trimmed = raw.trim();
        let lower = trimmed.to_ascii_lowercase(); if lower == "true" { return true.into(); } if lower == "false" { return false.into(); }
        if let Ok(v) = trimmed.parse::<i64>() { return v.into(); }
        if let Ok(v) = trimmed.parse::<rust_decimal::Decimal>() { return v.into(); }
        if let Ok(v) = trimmed.parse::<f64>() { return v.into(); }
        if let Ok(v) = uuid::Uuid::parse_str(trimmed) { return v.into(); }
        if let Some(v) = Self::parse_datetime(trimmed, false) { return v.into(); }
        trimmed.to_string().into()
    }
    fn parse_datetime(raw: &str, end_of_day: bool) -> Option<time::OffsetDateTime> {
        let trimmed = raw.trim();
        if let Ok(dt) = time::OffsetDateTime::parse(trimmed, &time::format_description::well_known::Rfc3339) { return Some(dt); }
        if trimmed.len() == 10 {
            let date = time::Date::parse(trimmed, &time::macros::format_description!("[year]-[month]-[day]")).ok()?;
            let t = if end_of_day { time::Time::from_hms(23, 59, 59).ok()? } else { time::Time::MIDNIGHT };
            return Some(date.with_time(t).assume_offset(time::UtcOffset::UTC));
        }
        None
    }
}
impl GeneratedTableAdapter for CompanyCryptoAccountTableAdapter {
    type Query<'db> = Query<'db, CompanyCryptoAccountModel>;
    type Row = CompanyCryptoAccountRecord;
    fn model_key(&self) -> &'static str { "CompanyCryptoAccount" }
    fn sortable_columns(&self) -> &'static [&'static str] { &["id", "crypto_network_id", "wallet_address", "conversion_rate", "status", "sort_order", "created_at", "updated_at"] }
    fn timestamp_columns(&self) -> &'static [&'static str] { &["created_at", "updated_at"] }
    fn column_descriptors(&self) -> &'static [DataTableColumnDescriptor] {
        &[
            DataTableColumnDescriptor { name: "id", label: "ID", data_type: "i64", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "crypto_network_id", label: "Crypto Network ID", data_type: "i64", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "wallet_address", label: "Wallet Address", data_type: "String", sortable: true, localized: false, filter_ops: &["eq", "like", "gte", "lte"] },
            DataTableColumnDescriptor { name: "conversion_rate", label: "Conversion Rate", data_type: "rust_decimal::Decimal", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "status", label: "Status", data_type: "CompanyCryptoAccountStatus", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "sort_order", label: "Sort Order", data_type: "i32", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte"] },
            DataTableColumnDescriptor { name: "created_at", label: "Created At", data_type: "time::OffsetDateTime", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte", "date_from", "date_to"] },
            DataTableColumnDescriptor { name: "updated_at", label: "Updated At", data_type: "time::OffsetDateTime", sortable: true, localized: false, filter_ops: &["eq", "gte", "lte", "date_from", "date_to"] },
        ]
    }
    fn relation_column_descriptors(&self) -> &'static [DataTableRelationColumnDescriptor] {
        &[
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "id", data_type: "i64", filter_ops: &["has_eq"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "name", data_type: "String", filter_ops: &["has_eq", "has_like"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "symbol", data_type: "String", filter_ops: &["has_eq", "has_like"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "status", data_type: "CryptoNetworkStatus", filter_ops: &["has_eq"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "sort_order", data_type: "i32", filter_ops: &["has_eq"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "created_at", data_type: "time::OffsetDateTime", filter_ops: &["has_eq"] },
            DataTableRelationColumnDescriptor { relation: "crypto_network", column: "updated_at", data_type: "time::OffsetDateTime", filter_ops: &["has_eq"] },
        ]
    }
    fn filter_patterns(&self) -> &'static [&'static str] {
        &[
            "f-<col>",
            "f-like-<col>",
            "f-gte-<col>",
            "f-lte-<col>",
            "f-date-from-<col>",
            "f-date-to-<col>",
            "f-like-any-<col1|col2|...>",
            "f-any-<col1|col2|...>",
            "f-has-<relation>-<col>",
            "f-has-like-<relation>-<col>",
        ]
    }
    fn apply_auto_filter<'db>(&self, query: Query<'db, CompanyCryptoAccountModel>, filter: &ParsedFilter, value: &str) -> anyhow::Result<Option<Query<'db, CompanyCryptoAccountModel>>> where Self: 'db {
        let trimmed = value.trim();
        if trimmed.is_empty() { return Ok(Some(query)); }
        match filter {
            ParsedFilter::Eq { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Eq, bind)))
            }
            ParsedFilter::Like { column } => {
                let Some(col) = Self::parse_like_col(column.as_str()) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Like, format!("%{}%", trimmed))))
            }
            ParsedFilter::Gte { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Ge, bind)))
            }
            ParsedFilter::Lte { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Le, bind)))
            }
            ParsedFilter::DateFrom { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(ts) = Self::parse_datetime(trimmed, false) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Ge, ts)))
            }
            ParsedFilter::DateTo { column } => {
                let Some(col) = Self::parse_col(column.as_str()) else { return Ok(None); };
                let Some(ts) = Self::parse_datetime(trimmed, true) else { return Ok(None); };
                Ok(Some(query.where_col(col, Op::Le, ts)))
            }
            ParsedFilter::LocaleEq { column } => {
                Ok(None)
            }
            ParsedFilter::LocaleLike { column } => {
                Ok(None)
            }
            ParsedFilter::LikeAny { columns } => {
                let mut applied = false;
                let pattern = format!("%{}%", trimmed);
                let next = query.where_group(|group| {
                    let mut q = group;
                    for column in columns {
                        if let Some(col) = Self::parse_like_col(column.as_str()) {
                            if applied { q = q.or_where_col(col, Op::Like, pattern.clone()); } else { q = q.where_col(col, Op::Like, pattern.clone()); applied = true; }
                        }
                    }
                    q
                });
                if applied { Ok(Some(next)) } else { Ok(None) }
            }
            ParsedFilter::Any { columns } => {
                let mut applied = false;
                let next = query.where_group(|group| {
                    let mut q = group;
                    for column in columns {
                        if let Some(col) = Self::parse_col(column.as_str()) {
                            if let Some(bind) = Self::parse_bind_for_col(column.as_str(), trimmed) { if applied { q = q.or_where_col(col, Op::Eq, bind.clone()); } else { q = q.where_col(col, Op::Eq, bind.clone()); applied = true; } }
                        }
                    }
                    q
                });
                if applied { Ok(Some(next)) } else { Ok(None) }
            }
            ParsedFilter::Has { relation, column } => {
                match (relation.as_str(), column.as_str()) {
                    ("crypto_network", "id") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "id", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Id, Op::Eq, bind)))) },
                    ("crypto_network", "name") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "name", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Name, Op::Eq, bind)))) },
                    ("crypto_network", "symbol") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "symbol", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Symbol, Op::Eq, bind)))) },
                    ("crypto_network", "status") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "status", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Status, Op::Eq, bind)))) },
                    ("crypto_network", "sort_order") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "sort_order", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::SortOrder, Op::Eq, bind)))) },
                    ("crypto_network", "created_at") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "created_at", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::CreatedAt, Op::Eq, bind)))) },
                    ("crypto_network", "updated_at") => { let Some(bind) = Self::parse_bind_for_relation("crypto_network", "updated_at", trimmed) else { return Ok(None); }; Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::UpdatedAt, Op::Eq, bind)))) },
                    _ => Ok(None),
                }
            }
            ParsedFilter::HasLike { relation, column } => {
                let pattern = format!("%{}%", trimmed);
                match (relation.as_str(), column.as_str()) {
                    ("crypto_network", "name") => Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Name, Op::Like, pattern.clone())))),
                    ("crypto_network", "symbol") => Ok(Some(query.where_has(CompanyCryptoAccountRel::CRYPTO_NETWORK, |rq| rq.where_col(CryptoNetworkDbCol::Symbol, Op::Like, pattern.clone())))),
                    _ => Ok(None),
                }
            }
            ParsedFilter::LocaleHas { relation, column } => {
                let Some(field) = Self::parse_locale_field_for_relation(relation.as_str(), column.as_str()) else { return Ok(None); };
                let locale = core_i18n::current_locale().to_string();
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
            ParsedFilter::LocaleHasLike { relation, column } => {
                let Some(field) = Self::parse_locale_field_for_relation(relation.as_str(), column.as_str()) else { return Ok(None); };
                let locale = core_i18n::current_locale().to_string();
                let pattern = format!("%{}%", trimmed);
                match (relation.as_str(), column.as_str()) {
                    _ => Ok(None),
                }
            }
        }
    }
    fn apply_sort<'db>(&self, query: Query<'db, CompanyCryptoAccountModel>, column: &str, dir: SortDirection) -> anyhow::Result<Query<'db, CompanyCryptoAccountModel>> where Self: 'db {
        let dir = match dir { SortDirection::Asc => OrderDir::Asc, SortDirection::Desc => OrderDir::Desc };
        let next = match column {
            "id" => query.order_by(CompanyCryptoAccountDbCol::Id, dir),
            "crypto_network_id" => query.order_by(CompanyCryptoAccountDbCol::CryptoNetworkId, dir),
            "wallet_address" => query.order_by(CompanyCryptoAccountDbCol::WalletAddress, dir),
            "conversion_rate" => query.order_by(CompanyCryptoAccountDbCol::ConversionRate, dir),
            "status" => query.order_by(CompanyCryptoAccountDbCol::Status, dir),
            "sort_order" => query.order_by(CompanyCryptoAccountDbCol::SortOrder, dir),
            "created_at" => query.order_by(CompanyCryptoAccountDbCol::CreatedAt, dir),
            "updated_at" => query.order_by(CompanyCryptoAccountDbCol::UpdatedAt, dir),
            _ => query,
        };
        Ok(next)
    }
    fn apply_cursor<'db>(&self, query: Query<'db, CompanyCryptoAccountModel>, column: &str, dir: SortDirection, cursor: &str) -> anyhow::Result<Option<Query<'db, CompanyCryptoAccountModel>>> where Self: 'db {
        let Some(col) = Self::parse_col(column) else { return Ok(None); };
        let Some(bind) = Self::parse_bind_for_col(column, cursor) else { return Ok(None); };
        let op = match dir { SortDirection::Asc => Op::Gt, SortDirection::Desc => Op::Lt };
        Ok(Some(query.where_col(col, op, bind)))
    }
    fn cursor_from_row(&self, row: &CompanyCryptoAccountRecord, column: &str) -> Option<String> {
        match column {
            "id" => Some(row.id.to_string()),
            "crypto_network_id" => Some(row.crypto_network_id.to_string()),
            "wallet_address" => Some(row.wallet_address.clone()),
            "conversion_rate" => Some(row.conversion_rate.to_string()),
            "sort_order" => Some(row.sort_order.to_string()),
            "created_at" => row.created_at.format(&time::format_description::well_known::Rfc3339).ok(),
            "updated_at" => row.updated_at.format(&time::format_description::well_known::Rfc3339).ok(),
            _ => None,
        }
    }
    fn count<'db>(&self, query: Query<'db, CompanyCryptoAccountModel>) -> BoxFuture<'db, anyhow::Result<i64>> where Self: 'db {
        Box::pin(async move { query.count().await })
    }
    fn fetch_page<'db>(&self, query: Query<'db, CompanyCryptoAccountModel>, page: i64, per_page: i64) -> BoxFuture<'db, anyhow::Result<Vec<CompanyCryptoAccountRecord>>> where Self: 'db {
        Box::pin(async move { Ok(query.paginate(page, per_page).await?.data) })
    }
}
#[derive(Debug, Clone, Copy)]
pub struct CompanyCryptoAccountDataTableConfig {
    pub default_sorting_column: &'static str,
    pub default_sorted: SortDirection,
    pub default_export_ignore_columns: &'static [&'static str],
    pub default_timestamp_columns: &'static [&'static str],
    pub default_unsortable: &'static [&'static str],
    pub default_row_per_page: Option<i64>,
}
impl Default for CompanyCryptoAccountDataTableConfig {
    fn default() -> Self {
        Self {
            default_sorting_column: "id",
            default_sorted: SortDirection::Desc,
            default_export_ignore_columns: &["actions", "action"],
            default_timestamp_columns: &["created_at", "updated_at"],
            default_unsortable: &[],
            default_row_per_page: None,
        }
    }
}
pub trait CompanyCryptoAccountDataTableHooks: Send + Sync + 'static {
    fn scope<'db>(&'db self, query: Query<'db, CompanyCryptoAccountModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> Query<'db, CompanyCryptoAccountModel> { query }
    fn authorize(&self, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<bool> { Ok(true) }
    fn filter_query<'db>(&'db self, _query: Query<'db, CompanyCryptoAccountModel>, _filter_key: &str, _value: &str, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<Option<Query<'db, CompanyCryptoAccountModel>>> { Ok(None) }
    fn filters<'db>(&'db self, query: Query<'db, CompanyCryptoAccountModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<Query<'db, CompanyCryptoAccountModel>> { Ok(query) }
    fn map_row(&self, _row: &mut CompanyCryptoAccountRecord, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<()> { Ok(()) }
    fn default_row_to_record(&self, row: CompanyCryptoAccountRecord) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> {
        let value = serde_json::to_value(&row)?;
        let mut record = match value { serde_json::Value::Object(map) => map, _ => anyhow::bail!("Generated row must serialize to a JSON object"), };
        if let Some(id_value) = record.get("id").cloned() {
            let id_text = match id_value {
                serde_json::Value::Number(number) => number.to_string(),
                serde_json::Value::String(text) => text,
                other => other.to_string(),
            };
            record.insert("id".to_string(), serde_json::Value::String(id_text));
        }
        Ok(record)
    }
    fn row_to_record(&self, row: CompanyCryptoAccountRecord, _input: &DataTableInput, _ctx: &DataTableContext) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> {
        self.default_row_to_record(row)
    }
    fn summary<'db>(&'db self, _query: Query<'db, CompanyCryptoAccountModel>, _input: &DataTableInput, _ctx: &DataTableContext) -> BoxFuture<'db, anyhow::Result<Option<serde_json::Value>>> { Box::pin(async { Ok(None) }) }
}
#[derive(Default)]
pub struct CompanyCryptoAccountDefaultDataTableHooks;
impl CompanyCryptoAccountDataTableHooks for CompanyCryptoAccountDefaultDataTableHooks {}
pub struct CompanyCryptoAccountDataTable<H = CompanyCryptoAccountDefaultDataTableHooks> where H: CompanyCryptoAccountDataTableHooks {
    pub db: sqlx::PgPool,
    pub hooks: H,
    pub config: CompanyCryptoAccountDataTableConfig,
    adapter: CompanyCryptoAccountTableAdapter,
}
impl CompanyCryptoAccountDataTable<CompanyCryptoAccountDefaultDataTableHooks> {
    pub fn new(db: sqlx::PgPool) -> Self {
        Self {
            db,
            hooks: CompanyCryptoAccountDefaultDataTableHooks,
            config: CompanyCryptoAccountDataTableConfig::default(),
            adapter: CompanyCryptoAccountTableAdapter,
        }
    }
}
impl<H: CompanyCryptoAccountDataTableHooks> CompanyCryptoAccountDataTable<H> {
    pub fn with_hooks<NH: CompanyCryptoAccountDataTableHooks>(self, hooks: NH) -> CompanyCryptoAccountDataTable<NH> {
        CompanyCryptoAccountDataTable {
            db: self.db,
            hooks,
            config: self.config,
            adapter: CompanyCryptoAccountTableAdapter,
        }
    }
    pub fn with_config(mut self, config: CompanyCryptoAccountDataTableConfig) -> Self {
        self.config = config;
        self
    }
}
impl<H: CompanyCryptoAccountDataTableHooks> AutoDataTable for CompanyCryptoAccountDataTable<H> {
    type Adapter = CompanyCryptoAccountTableAdapter;
    fn adapter(&self) -> &Self::Adapter { &self.adapter }
    fn base_query<'db>(&'db self, input: &DataTableInput, ctx: &DataTableContext) -> Query<'db, CompanyCryptoAccountModel> {
        self.hooks.scope(CompanyCryptoAccountModel::query(&self.db), input, ctx)
    }
    fn authorize(&self, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<bool> { self.hooks.authorize(input, ctx) }
    fn filter_query<'db>(&'db self, query: Query<'db, CompanyCryptoAccountModel>, filter_key: &str, value: &str, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<Option<Query<'db, CompanyCryptoAccountModel>>> { self.hooks.filter_query(query, filter_key, value, input, ctx) }
    fn filters<'db>(&'db self, query: Query<'db, CompanyCryptoAccountModel>, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<Query<'db, CompanyCryptoAccountModel>> { self.hooks.filters(query, input, ctx) }
    fn map_row(&self, row: &mut CompanyCryptoAccountRecord, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<()> { self.hooks.map_row(row, input, ctx) }
    fn row_to_record(&self, row: CompanyCryptoAccountRecord, input: &DataTableInput, ctx: &DataTableContext) -> anyhow::Result<serde_json::Map<String, serde_json::Value>> { self.hooks.row_to_record(row, input, ctx) }
    fn summary<'db>(&'db self, query: Query<'db, CompanyCryptoAccountModel>, input: &DataTableInput, ctx: &DataTableContext) -> BoxFuture<'db, anyhow::Result<Option<serde_json::Value>>> where Self: 'db { self.hooks.summary(query, input, ctx) }
    fn default_sorting_column(&self) -> &'static str { self.config.default_sorting_column }
    fn default_sorted(&self) -> SortDirection { self.config.default_sorted }
    fn default_export_ignore_columns(&self) -> &'static [&'static str] { self.config.default_export_ignore_columns }
    fn default_timestamp_columns(&self) -> &'static [&'static str] { self.config.default_timestamp_columns }
    fn default_unsortable(&self) -> &'static [&'static str] { self.config.default_unsortable }
    fn default_row_per_page(&self, ctx: &DataTableContext) -> i64 { self.config.default_row_per_page.unwrap_or(ctx.default_per_page) }
}
use core_db::common::active_record::ActiveRecord;
#[async_trait::async_trait]
impl ActiveRecord for CompanyCryptoAccountRecord {
    type Id = i64;
    async fn find(db: &sqlx::PgPool, id: Self::Id) -> anyhow::Result<Option<Self>> {
        CompanyCryptoAccountModel::find(db, id).await.map_err(|e| e.into())
    }
}
pub struct CompanyCryptoAccountModel;
impl CompanyCryptoAccountModel {
    pub const TABLE: &'static str = "company_crypto_accounts";
    pub const MODEL_KEY: &'static str = "company_crypto_account";
    pub const PK: &'static str = "id";
    pub fn query<'db>(db: impl Into<DbConn<'db>>) -> Query<'db, CompanyCryptoAccountModel> {
        Query::new(db)
    }
    pub fn query_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Query<'db, CompanyCryptoAccountModel> {
        Query::new_with_base_url(db, base_url)
    }
    pub fn create<'db>(db: impl Into<DbConn<'db>>) -> Create<'db, CompanyCryptoAccountModel> {
        Create::new(db)
    }
    pub fn create_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Create<'db, CompanyCryptoAccountModel> {
        Create::new_with_base_url(db, base_url)
    }
    pub fn patch<'db>(db: impl Into<DbConn<'db>>) -> Patch<'db, CompanyCryptoAccountModel> {
        Patch::new(db)
    }
    pub fn patch_with_base_url<'db>(db: impl Into<DbConn<'db>>, base_url: Option<String>) -> Patch<'db, CompanyCryptoAccountModel> {
        Patch::new_with_base_url(db, base_url)
    }
    pub async fn find<'db>(db: impl Into<DbConn<'db>>, id: i64) -> Result<Option<CompanyCryptoAccountRecord>> {
        Query::<CompanyCryptoAccountModel>::new(db).find(id).await
    }
    fn _transform_create_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        let _ = col;
        Ok(value)
    }
    fn _transform_patch_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        let _ = col;
        Ok(value)
    }
}

impl ModelDef for CompanyCryptoAccountModel {
    type Pk = i64;
    type Record = CompanyCryptoAccountRecord;
    type Create = CompanyCryptoAccountCreate;
    type Changes = CompanyCryptoAccountChanges;
    const TABLE: &'static str = CompanyCryptoAccountModel::TABLE;
    const MODEL_KEY: &'static str = CompanyCryptoAccountModel::MODEL_KEY;
    const PK_COL: &'static str = "id";
}

impl core_db::common::model_api::QueryModel for CompanyCryptoAccountModel {
    const DEFAULT_SELECT: &'static str = "id, crypto_network_id, wallet_address, conversion_rate, status, sort_order, created_at, updated_at";
    const HAS_SOFT_DELETE: bool = false;
    const SOFT_DELETE_COL: &'static str = "";
    const HAS_CREATED_AT: bool = true;
    const HAS_UPDATED_AT: bool = true;
    fn query_all<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Vec<Self::Record>> {
        Box::pin(async move {
            let (sql, binds) = state.to_select_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_as::<_, CompanyCryptoAccountRow>(&sql);
            for b in binds { q = bind(q, b); }
            let rows = state.db.fetch_all(q).await?;
            record_profiled_query("company_crypto_accounts", "SELECT", &sql, &__profiler_binds, __profiler_start.elapsed());
            let db = state.db.clone();
            let crypto_network = load_crypto_network(db.clone(), &rows).await?;
            let ids: Vec<i64> = rows.iter().map(|r| r.id.clone()).collect();
            let localized = LocalizedMap::default();
            let mut out_vec = Vec::with_capacity(rows.len());
            for r in rows {
                let key = r.id.clone();
                let mut record = hydrate_record(r.clone(), &LocalizedMap::default(), state.base_url.as_deref());
                record.crypto_network = crypto_network.get(&key).cloned().unwrap_or(None);
                out_vec.push(record);
            }
            Ok(out_vec)
        })
    }
    fn query_first<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Option<Self::Record>> {
        Box::pin(async move {
            let mut v = Self::query_all(state.limit(1)).await?;
            Ok(v.pop())
        })
    }
    fn query_find<'db>(state: QueryState<'db>, id: Self::Pk) -> core_db::common::model_api::BoxModelFuture<'db, Option<Self::Record>> {
        Box::pin(async move { Self::query_first(state.where_col_str("id", Op::Eq, id.into())).await })
    }
    fn query_count<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, i64> {
        Box::pin(async move {
            let (sql, binds) = state.to_count_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_scalar::<_, i64>(&sql);
            for b in binds { q = bind_scalar(q, b); }
            let count = state.db.fetch_scalar(q).await?;
            record_profiled_query("company_crypto_accounts", "COUNT", &sql, &__profiler_binds, __profiler_start.elapsed());
            Ok(count)
        })
    }
    fn query_delete<'db>(state: QueryState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, u64> {
        Box::pin(async move {
            if state.limit.is_some() {
                anyhow::bail!("delete() does not support limit; add where clauses");
            }
            let db = state.db;
            let mut where_sql = state.where_sql;
            let binds = state.binds;
            if where_sql.is_empty() { anyhow::bail!("delete(): no conditions set"); }
            let __profiler_binds = if is_sql_profiler_enabled() { binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __observer_active = try_get_observer().is_some();
            let __old_rows: Vec<CompanyCryptoAccountRow> = if __observer_active {
                let select_sql = format!("SELECT * FROM company_crypto_accounts WHERE {}", where_sql.join(" AND "));
                let mut fq = sqlx::query_as::<_, CompanyCryptoAccountRow>(&select_sql);
                for b in &binds { fq = bind(fq, b.clone()); }
                let rows: Vec<CompanyCryptoAccountRow> = db.fetch_all(fq).await.unwrap_or_default();
                rows
            } else {
                Vec::new()
            };
            if !__old_rows.is_empty() {
                if let Some(observer) = try_get_observer() {
                    for old_row in &__old_rows {
                        let old_data = serde_json::to_value(old_row)?;
                        let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", old_row.id)) };
                        observer.on_deleting(&event, &old_data).await?;
                    }
                }
            }
            let mut sql = String::from("DELETE FROM company_crypto_accounts");
            if !where_sql.is_empty() {
                sql.push_str(" WHERE ");
                sql.push_str(&where_sql.join(" AND "));
            }
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query(&sql);
            for b in binds { q = bind_query(q, b); }
            let res = db.execute(q).await?;
            record_profiled_query("company_crypto_accounts", "DELETE", &sql, &__profiler_binds, __profiler_start.elapsed());
            if !__old_rows.is_empty() && res.rows_affected() > 0 {
                if let Some(observer) = try_get_observer() {
                    for old_row in &__old_rows {
                        let event = ModelEvent { model: "company_crypto_account", table: "company_crypto_accounts", record_key: Some(format!("{}", old_row.id)) };
                        match serde_json::to_value(old_row) {
                            Ok(old_data) => {
                                if let Err(err) = observer.on_deleted(&event, &old_data).await {
                                    log_observer_error("deleted", "company_crypto_account", &err);
                                }
                            }
                            Err(err) => log_observer_error("deleted", "company_crypto_account", &err),
                        }
                    }
                }
            }
            Ok(res.rows_affected())
        })
    }
    fn query_paginate<'db>(state: QueryState<'db>, page: i64, per_page: i64) -> core_db::common::model_api::BoxModelFuture<'db, core_db::common::model_api::Page<Self::Record>> {
        Box::pin(async move {
            let page = if page < 1 { 1 } else { page };
            let per_page = resolve_per_page(per_page);
            let (count_sql, count_binds) = state.to_count_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_binds = if is_sql_profiler_enabled() { count_binds.iter().map(|b| format!("{}", b)).collect::<Vec<_>>().join(", ") } else { String::new() };
            let __profiler_start = std::time::Instant::now();
            let mut count_q = sqlx::query_scalar::<_, i64>(&count_sql);
            for b in count_binds { count_q = bind_scalar(count_q, b); }
            let total: i64 = state.db.fetch_scalar(count_q).await?;
            record_profiled_query("company_crypto_accounts", "COUNT", &count_sql, &__profiler_binds, __profiler_start.elapsed());
            let last_page = ((total + per_page - 1) / per_page).max(1);
            let current_page = page.min(last_page);
            let offset_val = (current_page - 1) * per_page;
            let mut state = state;
            state.offset = Some(offset_val);
            state.limit = Some(per_page);
            let (sql, binds) = state.to_select_sql(Self::TABLE, Self::HAS_SOFT_DELETE, Self::SOFT_DELETE_COL);
            let __profiler_start = std::time::Instant::now();
            let mut q = sqlx::query_as::<_, CompanyCryptoAccountRow>(&sql);
            for b in binds { q = bind(q, b); }
            let rows = state.db.fetch_all(q).await?;
            record_profiled_query("company_crypto_accounts", "SELECT", &sql, &__profiler_binds, __profiler_start.elapsed());
            let db = state.db.clone();
            let crypto_network = load_crypto_network(db.clone(), &rows).await?;
            let ids: Vec<i64> = rows.iter().map(|r| r.id.clone()).collect();
            let localized = LocalizedMap::default();
            let mut data = Vec::with_capacity(rows.len());
            for r in rows {
                let key = r.id.clone();
                let mut record = hydrate_record(r.clone(), &LocalizedMap::default(), state.base_url.as_deref());
                record.crypto_network = crypto_network.get(&key).cloned().unwrap_or(None);
                data.push(record);
            }
            Ok(core_db::common::model_api::Page { data, total, per_page, current_page, last_page })
        })
    }
}

impl core_db::common::model_api::CreateModel for CompanyCryptoAccountModel {
    fn create_save<'db>(state: CreateState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Self::Record> {
        Box::pin(async move {
            let builder = CompanyCryptoAccountCreateInner::from_state(state);
            let db = builder.state.db.clone();
            let base_url = builder.state.base_url.clone();
            let created = builder.save().await?;
            Query::<CompanyCryptoAccountModel>::new_with_base_url(db, base_url).find(created.id.clone()).await?.ok_or_else(|| anyhow::anyhow!("company_crypto_accounts: created record not found"))
        })
    }
    fn transform_create_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        Self::_transform_create_value(col, value)
    }
}

impl core_db::common::model_api::CreateField<CompanyCryptoAccountModel> for CompanyCryptoAccountDbCol {
    type Value = BindValue;
    fn set<'db>(field: Self, state: CreateState<'db>, value: BindValue) -> anyhow::Result<CreateState<'db>> {
        let value = <CompanyCryptoAccountModel as core_db::common::model_api::CreateModel>::transform_create_value(field.as_sql(), value)?;
        Ok(state.set_col(field.as_sql(), value))
    }
}

impl core_db::common::model_api::CreateConflictField<CompanyCryptoAccountModel> for CompanyCryptoAccountDbCol {
    fn on_conflict_do_nothing<'db>(state: CreateState<'db>, fields: &[Self]) -> CreateState<'db> {
        let cols: Vec<&'static str> = fields.iter().map(|f| f.as_sql()).collect();
        state.on_conflict_do_nothing(&cols)
    }
    fn on_conflict_update<'db>(state: CreateState<'db>, fields: &[Self]) -> CreateState<'db> {
        let cols: Vec<&'static str> = fields.iter().map(|f| f.as_sql()).collect();
        state.on_conflict_update(&cols)
    }
}

impl core_db::common::model_api::PatchModel for CompanyCryptoAccountModel {
    fn patch_from_query<'db>(mut state: QueryState<'db>) -> PatchState<'db> {
        let db = state.db.clone();
        let base_url = state.base_url.clone();
        state.select_sql = Some(CompanyCryptoAccountDbCol::Id.as_sql().to_string());
        let (scope_sql, binds) = state.to_sql();
        let mut ps = PatchState::new(db, base_url, "company_crypto_accounts");
        ps.where_sql.push(format!("{} IN ({})", CompanyCryptoAccountDbCol::Id.as_sql(), scope_sql));
        ps.where_binds = binds;
        ps
    }
    fn patch_save<'db>(state: PatchState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, u64> {
        Box::pin(async move {
            let builder = CompanyCryptoAccountPatchInner::from_state(state);
            builder.save().await
        })
    }
    fn patch_fetch<'db>(state: PatchState<'db>) -> core_db::common::model_api::BoxModelFuture<'db, Vec<Self::Record>> {
        Box::pin(async move {
            if state.where_sql.is_empty() {
                anyhow::bail!("update: no conditions set");
            }
            let db = state.db.clone();
            let base_url = state.base_url.clone();
            let mut select_sql = format!("SELECT {} FROM company_crypto_accounts", CompanyCryptoAccountDbCol::Id.as_sql());
            select_sql.push_str(&format!(" WHERE {}", state.where_sql.join(" AND ")));
            let mut select_q = sqlx::query_scalar::<_, i64>(&select_sql);
            for bind_value in &state.where_binds { select_q = bind_scalar(select_q, bind_value.clone()); }
            let target_ids = db.fetch_all_scalar(select_q).await?;
            let builder = CompanyCryptoAccountPatchInner::from_state(state);
            builder.save().await?;
            if target_ids.is_empty() {
                return Ok(Vec::new());
            }
            let query = Query::<CompanyCryptoAccountModel>::new_with_base_url(db, base_url);
            let binds: Vec<BindValue> = target_ids.iter().cloned().map(Into::into).collect();
            let state = query.into_inner().where_in_str(CompanyCryptoAccountDbCol::Id.as_sql(), &binds);
            <Self as core_db::common::model_api::QueryModel>::query_all(state).await
        })
    }
    fn transform_patch_value(col: &str, value: BindValue) -> anyhow::Result<BindValue> {
        Self::_transform_patch_value(col, value)
    }
}

impl core_db::common::model_api::PatchAssignField<CompanyCryptoAccountModel> for CompanyCryptoAccountDbCol {
    type Value = BindValue;
    fn assign<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        let value = <CompanyCryptoAccountModel as core_db::common::model_api::PatchModel>::transform_patch_value(field.as_sql(), value)?;
        Ok(state.assign_col(field.as_sql(), value))
    }
}

impl core_db::common::model_api::PatchNumericField<CompanyCryptoAccountModel> for CompanyCryptoAccountDbCol {
    fn increment<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        match field {
            CompanyCryptoAccountDbCol::Id => Ok(state.increment_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::CryptoNetworkId => Ok(state.increment_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::ConversionRate => Ok(state.increment_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::SortOrder => Ok(state.increment_col(field.as_sql(), value)),
            _ => anyhow::bail!("column '{}' does not support increment", field.as_sql()),
        }
    }
    fn decrement<'db>(field: Self, state: PatchState<'db>, value: BindValue) -> anyhow::Result<PatchState<'db>> {
        match field {
            CompanyCryptoAccountDbCol::Id => Ok(state.decrement_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::CryptoNetworkId => Ok(state.decrement_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::ConversionRate => Ok(state.decrement_col(field.as_sql(), value)),
            CompanyCryptoAccountDbCol::SortOrder => Ok(state.decrement_col(field.as_sql(), value)),
            _ => anyhow::bail!("column '{}' does not support decrement", field.as_sql()),
        }
    }
}

impl core_db::common::model_api::ColExpr for CompanyCryptoAccountDbCol {
    fn col_sql(self) -> &'static str { self.as_sql() }
}

impl core_db::common::model_api::QueryField<CompanyCryptoAccountModel> for CompanyCryptoAccountDbCol {
    type Value = BindValue;
    fn where_col<'db>(field: Self, state: QueryState<'db>, op: Op, value: BindValue) -> QueryState<'db> {
        state.where_col_str(field.as_sql(), op, value)
    }
    fn or_where_col<'db>(field: Self, state: QueryState<'db>, op: Op, value: BindValue) -> QueryState<'db> {
        state.or_where_col_str(field.as_sql(), op, value)
    }
    fn where_in<'db>(field: Self, state: QueryState<'db>, values: &[BindValue]) -> QueryState<'db> {
        state.where_in_str(field.as_sql(), values)
    }
    fn order_by<'db>(field: Self, state: QueryState<'db>, dir: OrderDir) -> QueryState<'db> {
        state.order_by_str(field.as_sql(), dir)
    }
    fn where_null<'db>(field: Self, state: QueryState<'db>) -> QueryState<'db> {
        state.where_null_str(field.as_sql())
    }
    fn where_not_null<'db>(field: Self, state: QueryState<'db>) -> QueryState<'db> {
        state.where_not_null_str(field.as_sql())
    }
}

impl core_db::common::model_api::IncludeRelation<CompanyCryptoAccountModel> for OneRelation<CompanyCryptoAccountModel, CryptoNetworkRow, 0> {
    fn include<'db>(_relation: Self, state: QueryState<'db>) -> QueryState<'db> {
        state
    }
}

impl core_db::common::model_api::WhereHasRelation<CompanyCryptoAccountModel> for OneRelation<CompanyCryptoAccountModel, CryptoNetworkRow, 0> {
    type Target = CryptoNetworkModel;
    fn where_has<'db, F>(_relation: Self, mut state: QueryState<'db>, scope: F) -> QueryState<'db>
    where
        F: FnOnce(Query<'db, Self::Target>) -> Query<'db, Self::Target>,
    {
        let start_idx = state.binds.len() + 1;
        let scoped = scope(CryptoNetworkModel::query_with_base_url(state.db.clone(), None));
        let (mut sub_where, sub_binds) = scoped.into_inner().into_where_parts();
        sub_where.insert(0, "crypto_networks.id = company_crypto_accounts.crypto_network_id".to_string());
        let mut clause = String::from("EXISTS (SELECT 1 FROM crypto_networks WHERE ");
        clause.push_str(&sub_where.join(" AND "));
        clause.push(')');
        let clause = renumber_placeholders(&clause, start_idx);
        state.where_sql.push(clause);
        state.binds.extend(sub_binds);
        state
    }
    fn or_where_has<'db, F>(_relation: Self, mut state: QueryState<'db>, scope: F) -> QueryState<'db>
    where
        F: FnOnce(Query<'db, Self::Target>) -> Query<'db, Self::Target>,
    {
        let start_idx = state.binds.len() + 1;
        let scoped = scope(CryptoNetworkModel::query_with_base_url(state.db.clone(), None));
        let (mut sub_where, sub_binds) = scoped.into_inner().into_where_parts();
        sub_where.insert(0, "crypto_networks.id = company_crypto_accounts.crypto_network_id".to_string());
        let mut clause = String::from("EXISTS (SELECT 1 FROM crypto_networks WHERE ");
        clause.push_str(&sub_where.join(" AND "));
        clause.push(')');
        let clause = renumber_placeholders(&clause, start_idx);
        if let Some(last) = state.where_sql.pop() {
            state.where_sql.push(format!("({} OR {})", last, clause));
        } else {
            state.where_sql.push(clause);
        }
        state.binds.extend(sub_binds);
        state
    }
}

impl core_db::common::model_api::RecordOneRelation<CompanyCryptoAccountModel> for OneRelation<CompanyCryptoAccountModel, CryptoNetworkRow, 0> {
    type Target = CryptoNetworkRow;
    fn get<'a>(_relation: Self, record: &'a CompanyCryptoAccountRecord) -> Option<&'a Self::Target> {
        record.crypto_network.as_ref()
    }
}


